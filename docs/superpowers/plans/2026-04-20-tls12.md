# TLS 1.2 Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 TLS 1.3 纯 Rust 实现基础上添加 TLS 1.2 支持，ClientHello 同时宣告两个版本，`TlsStream::connect` 根据 ServerHello 自动选择握手路径。

**Architecture:** ClientHello 的 `supported_versions` 扩展同时列出 `0x0304`（TLS 1.3）和 `0x0303`（TLS 1.2）。解析 ServerHello 后判断版本，进入 `connect_tls13`（现有逻辑）或 `connect_tls12`（新逻辑）。TLS 1.2 特有代码隔离在 `tls12/` 子目录；现有 TLS 1.3 代码整理进 `tls13/`，通过 re-export 保持测试路径不变。

**Tech Stack:** 纯 Rust，零新依赖。密码原语全部复用：P-256（`p256.rs`）、AES-128-GCM（`crypto/aes_gcm.rs`）、HMAC-SHA256（`crypto/sha256.rs`）、RSA/ECDSA 验签（`rsa.rs` / `ecdsa.rs`）。

---

## 文件变更总览

**新建：**
- `src/core/net/tls/tls13/mod.rs` — 声明 tls13 子模块
- `src/core/net/tls/tls13/handshake.rs` — 从 `handshake.rs` 移入，新增 `client_random()` 方法，`HandshakeResult` 增加 `NegotiatedTls12` 变体
- `src/core/net/tls/tls13/key_schedule.rs` — 从 `key_schedule.rs` 移入
- `src/core/net/tls/tls13/record.rs` — 从 `record.rs` 移入
- `src/core/net/tls/tls12/mod.rs` — 声明 tls12 子模块 + `connect_tls12` 函数
- `src/core/net/tls/tls12/key_schedule.rs` — PRF、master_secret、key_block、finished_verify_data
- `src/core/net/tls/tls12/record.rs` — `Tls12RecordLayer`（显式 IV）
- `src/core/net/tls/tls12/handshake.rs` — `Tls12Handshake` 状态机
- `tests/core/net/tls/tls12/mod.rs`
- `tests/core/net/tls/tls12/key_schedule_test.rs`
- `tests/core/net/tls/tls12/record_test.rs`
- `tests/core/net/tls/tls12/handshake_test.rs`

**修改：**
- `src/core/net/tls/mod.rs` — 添加 `tls12`/`tls13` 模块声明，re-export 保持旧路径，`TlsStream` 字段从 `RecordLayer` 改为 `RecordLayerVariant`，`connect` 分叉逻辑
- `src/core/net/tls/codec.rs` — `supported_versions` 增加 0x0303，cipher_suites 增加 0xC02B/0xC02C，supported_groups 增加 secp256r1，`ServerHello` 增加 `version` 字段，`decode_server_hello` 处理 TLS 1.2，新增 `ServerKeyExchange`/`encode_client_key_exchange`/`peek_server_hello_version`
- `src/core/net/tls/p256.rs` — 新增 `to_uncompressed()` 方法
- `tests/core/net/tls/mod.rs` — 添加 `pub mod tls12;`
- `tests/core/net/tls_test.rs` — 新增 `tls12_pure_rust_https_get`

**删除：**
- `src/core/net/tls/handshake.rs`
- `src/core/net/tls/key_schedule.rs`
- `src/core/net/tls/record.rs`

---

## Task 1: 将 TLS 1.3 代码整理进 tls13/ 子目录

**Files:**
- Create: `src/core/net/tls/tls13/mod.rs`
- Create: `src/core/net/tls/tls13/handshake.rs` (从 `handshake.rs` 移入)
- Create: `src/core/net/tls/tls13/key_schedule.rs` (从 `key_schedule.rs` 移入)
- Create: `src/core/net/tls/tls13/record.rs` (从 `record.rs` 移入)
- Modify: `src/core/net/tls/mod.rs`
- Delete: `src/core/net/tls/handshake.rs`, `key_schedule.rs`, `record.rs`

- [ ] **Step 1: 创建 tls13/ 目录和 mod.rs**

```rust
// src/core/net/tls/tls13/mod.rs
pub mod handshake;
pub mod key_schedule;
pub mod record;
```

- [ ] **Step 2: 将 handshake.rs 移入 tls13/，修正 use 路径**

将 `src/core/net/tls/handshake.rs` 内容复制到 `src/core/net/tls/tls13/handshake.rs`，修改以下 import：

```rust
// 替换所有 super:: 开头的跨模块 import：
use crate::core::net::tls::codec::{self, HandshakeMessage};
use crate::core::net::tls::crypto::sha256::{Sha256, hmac_sha256};
use crate::core::net::tls::crypto::x25519;
// 同模块内引用保持 super::
use super::key_schedule::KeySchedule;
use super::record::RecordLayer;
```

在 `Handshake` 结构体末尾添加 `client_random()` 方法（Task 8 需要）：

```rust
/// Expose client random for TLS 1.2 fallback.
pub fn client_random(&self) -> &[u8; 32] {
    &self.random
}
```

同时，将 `HandshakeResult` 增加一个变体（Task 8 需要）：

```rust
pub enum HandshakeResult {
    Continue,
    Complete,
    NegotiatedTls12 { server_random: [u8; 32], cipher_suite: u16 },
}
```

在 `handle_message` 的 `ExpectServerHello` 分支，在检查 cipher_suite 之前加版本判断：

```rust
(State::ExpectServerHello, HandshakeMessage::ServerHello(sh)) => {
    self.transcript.update(msg_bytes);

    // TLS 1.2 negotiated — caller handles the rest
    if sh.version == 0x0303 {
        return Ok(HandshakeResult::NegotiatedTls12 {
            server_random: sh.random,
            cipher_suite: sh.cipher_suite,
        });
    }

    // 原有 TLS 1.3 逻辑（cipher_suite 检查等）不变
    if sh.cipher_suite != 0x1301 { ... }
    ...
}
```

- [ ] **Step 3: 将 key_schedule.rs 移入 tls13/**

将 `src/core/net/tls/key_schedule.rs` 内容复制到 `src/core/net/tls/tls13/key_schedule.rs`，修改 import：

```rust
use crate::core::net::tls::crypto::sha256::{Sha256, hkdf_expand, hkdf_extract};
```

- [ ] **Step 4: 将 record.rs 移入 tls13/**

将 `src/core/net/tls/record.rs` 内容复制到 `src/core/net/tls/tls13/record.rs`，修改 import：

```rust
use crate::core::net::tls::crypto::aes_gcm::Aes128Gcm;
use crate::core::net::tls::codec::{APPLICATION_DATA, ALERT, HANDSHAKE};
```

将 `super::codec::APPLICATION_DATA` 等所有 `super::codec::` 引用改为直接用 import 的名称。

- [ ] **Step 5: 更新 tls/mod.rs**

在 `mod.rs` 顶部的模块声明处替换：

```rust
// 删除旧声明（这些文件将不再存在）：
// mod handshake;  ← 删除
// mod key_schedule;  ← 删除  
// mod record;  ← 删除

// 新增：
pub mod tls13;

// 为保持现有测试路径 viv::core::net::tls::{handshake,key_schedule,record} 有效：
pub use tls13::handshake;
pub use tls13::key_schedule;
pub use tls13::record;
```

将 `mod.rs` 内所有 `use handshake::` 改为 `use tls13::handshake::`，
`use key_schedule::` 改为 `use tls13::key_schedule::`，
`use record::` 改为 `use tls13::record::`。

- [ ] **Step 6: 删除旧文件**

```bash
rm src/core/net/tls/handshake.rs
rm src/core/net/tls/key_schedule.rs
rm src/core/net/tls/record.rs
```

- [ ] **Step 7: 验证无编译错误**

```bash
cargo build 2>&1 | grep "^error"
```

Expected: 无输出（零错误）。

- [ ] **Step 8: 运行全量测试确认无回归**

```bash
cargo test 2>&1 | tail -5
```

Expected: `test result: ok. N passed; 0 failed`

- [ ] **Step 9: Commit**

```bash
git add src/core/net/tls/tls13/ src/core/net/tls/mod.rs
git rm src/core/net/tls/handshake.rs src/core/net/tls/key_schedule.rs src/core/net/tls/record.rs
git commit -m "refactor(tls): 将 TLS 1.3 代码整理进 tls13/ 子目录"
```

---

## Task 2: 扩展 ClientHello 支持 TLS 1.2，解析 TLS 1.2 ServerHello

**Files:**
- Modify: `src/core/net/tls/codec.rs`
- Test: `tests/core/net/tls/codec_test.rs`

- [ ] **Step 1: 写失败测试**

在 `tests/core/net/tls/codec_test.rs` 末尾追加：

```rust
#[test]
fn client_hello_offers_tls12_cipher_suites() {
    use viv::core::net::tls::codec;
    let random = [0u8; 32];
    let session_id = [0u8; 32];
    let x25519_pub = [0u8; 32];
    let mut out = Vec::new();
    codec::encode_client_hello(&random, &session_id, "example.com", &x25519_pub, &mut out);

    // Find cipher_suites in the body (after type(1)+len(3)+legacy_version(2)+random(32)+sid(33))
    // Search for 0xC02B in the output bytes
    let found_c02b = out.windows(2).any(|w| w == [0xC0, 0x2B]);
    let found_c02c = out.windows(2).any(|w| w == [0xC0, 0x2C]);
    assert!(found_c02b, "TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256 (0xC02B) not in ClientHello");
    assert!(found_c02c, "TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256 (0xC02C) not in ClientHello");
}

#[test]
fn client_hello_supported_versions_includes_tls12() {
    use viv::core::net::tls::codec;
    let random = [0u8; 32];
    let session_id = [0u8; 32];
    let x25519_pub = [0u8; 32];
    let mut out = Vec::new();
    codec::encode_client_hello(&random, &session_id, "example.com", &x25519_pub, &mut out);

    // 0x0303 must appear in supported_versions extension
    let found_v12 = out.windows(2).any(|w| w == [0x03, 0x03]);
    assert!(found_v12, "TLS 1.2 version 0x0303 not in ClientHello");
}

#[test]
fn decode_tls12_server_hello_extracts_version() {
    use viv::core::net::tls::codec;
    // Minimal TLS 1.2 ServerHello (no key_share, no supported_versions ext)
    // legacy_version=0x0303, random(32), sid_len=0, cipher=0xC02B, comp=0, exts_len=0
    let mut body = Vec::new();
    body.extend_from_slice(&[0x03, 0x03]); // legacy_version
    body.extend_from_slice(&[0x42u8; 32]); // random
    body.push(0); // session_id length = 0
    body.extend_from_slice(&[0xC0, 0x2B]); // cipher_suite
    body.push(0); // compression
    body.extend_from_slice(&[0x00, 0x00]); // empty extensions

    // Wrap in handshake header: SERVER_HELLO(2) + len(3) + body
    let mut msg = Vec::new();
    msg.push(0x02);
    let len = body.len() as u32;
    msg.push((len >> 16) as u8);
    msg.push((len >> 8) as u8);
    msg.push(len as u8);
    msg.extend_from_slice(&body);

    let decoded = codec::decode_handshake(&msg).unwrap();
    if let codec::HandshakeMessage::ServerHello(sh) = decoded {
        assert_eq!(sh.version, 0x0303, "Expected TLS 1.2 version");
        assert!(sh.x25519_public.is_none(), "TLS 1.2 ServerHello has no key_share");
    } else {
        panic!("expected ServerHello");
    }
}
```

- [ ] **Step 2: 验证测试失败**

```bash
cargo test client_hello_offers_tls12 client_hello_supported_versions decode_tls12 -- --nocapture 2>&1 | grep -E "FAILED|error"
```

Expected: 3 tests fail（编译失败或断言失败）。

- [ ] **Step 3: 修改 ServerHello 结构体，增加 version 字段**

在 `src/core/net/tls/codec.rs` 中，将 `ServerHello` 改为：

```rust
pub struct ServerHello {
    pub version: u16,                       // 0x0303 TLS1.2 or 0x0304 TLS1.3
    pub random: [u8; 32],
    pub cipher_suite: u16,
    pub x25519_public: Option<[u8; 32]>,    // TLS 1.3 only
}
```

- [ ] **Step 4: 修改 decode_server_hello 处理两个版本**

将 `decode_server_hello` 中现有的 extensions 解析替换为：

```rust
fn decode_server_hello(body: &[u8]) -> crate::Result<HandshakeMessage> {
    if body.len() < 35 {
        return Err(crate::Error::Tls("ServerHello too short".into()));
    }
    let mut pos = 0;
    let legacy_version = read_u16(body, pos)?;
    pos += 2;
    let mut random = [0u8; 32];
    random.copy_from_slice(&body[pos..pos + 32]);
    pos += 32;
    let sid_len = body[pos] as usize;
    pos += 1 + sid_len;
    if pos + 3 > body.len() {
        return Err(crate::Error::Tls("ServerHello truncated at cipher".into()));
    }
    let cipher_suite = read_u16(body, pos)?;
    pos += 2;
    pos += 1; // compression

    let mut x25519_public: Option<[u8; 32]> = None;
    let mut negotiated_version = legacy_version;

    if pos + 2 <= body.len() {
        let exts_len = read_u16(body, pos)? as usize;
        pos += 2;
        let exts_end = pos + exts_len;
        if exts_end > body.len() {
            return Err(crate::Error::Tls("ServerHello extensions truncated".into()));
        }
        while pos + 4 <= exts_end {
            let ext_type = read_u16(body, pos)?;
            let ext_len = read_u16(body, pos + 2)? as usize;
            pos += 4;
            if pos + ext_len > exts_end {
                return Err(crate::Error::Tls("extension data truncated".into()));
            }
            match ext_type {
                EXT_KEY_SHARE if ext_len >= 4 => {
                    let key_len = read_u16(body, pos + 2)? as usize;
                    if key_len == 32 && pos + 4 + 32 <= exts_end {
                        let mut k = [0u8; 32];
                        k.copy_from_slice(&body[pos + 4..pos + 4 + 32]);
                        x25519_public = Some(k);
                    }
                }
                EXT_SUPPORTED_VERSIONS if ext_len >= 2 => {
                    negotiated_version = read_u16(body, pos)?;
                    if negotiated_version != 0x0304 && negotiated_version != 0x0303 {
                        return Err(crate::Error::Tls(format!(
                            "unsupported TLS version: 0x{:04x}", negotiated_version
                        )));
                    }
                }
                _ => {}
            }
            pos += ext_len;
        }
    }

    if negotiated_version == 0x0304 && x25519_public.is_none() {
        return Err(crate::Error::Tls("TLS 1.3 ServerHello missing key_share".into()));
    }

    Ok(HandshakeMessage::ServerHello(ServerHello {
        version: negotiated_version,
        random,
        cipher_suite,
        x25519_public,
    }))
}
```

- [ ] **Step 5: 更新 encode_ext_supported_versions，同时宣告 TLS 1.2**

```rust
fn encode_ext_supported_versions(out: &mut Vec<u8>) {
    push_u16(out, EXT_SUPPORTED_VERSIONS);
    push_u16(out, 5); // extension data length: list_len(1) + 2 versions * 2 bytes
    out.push(4);      // list length
    push_u16(out, 0x0304); // TLS 1.3
    push_u16(out, 0x0303); // TLS 1.2
}
```

- [ ] **Step 6: 更新 encode_client_hello，添加 TLS 1.2 cipher suites**

将 cipher_suites 段改为：

```rust
// cipher_suites: length(2) + 3 suites * 2 bytes
push_u16(&mut body, 6);
push_u16(&mut body, 0x1301); // TLS_AES_128_GCM_SHA256 (TLS 1.3)
push_u16(&mut body, 0xC02B); // TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
push_u16(&mut body, 0xC02C); // TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
```

- [ ] **Step 7: 更新 encode_ext_supported_groups，添加 secp256r1**

```rust
fn encode_ext_supported_groups(out: &mut Vec<u8>) {
    push_u16(out, EXT_SUPPORTED_GROUPS);
    push_u16(out, 6); // ext data length
    push_u16(out, 4); // list length
    push_u16(out, 0x001d); // x25519
    push_u16(out, 0x0017); // secp256r1 (P-256) for TLS 1.2
}
```

- [ ] **Step 8: 新增 peek_server_hello_version 辅助函数（Task 8 需要）**

```rust
/// Extract the negotiated version from a raw ServerHello handshake message.
/// Returns 0x0303 for TLS 1.2, 0x0304 for TLS 1.3.
pub fn peek_server_hello_version(msg_bytes: &[u8]) -> crate::Result<u16> {
    // msg_bytes = type(1) + len(3) + body
    if msg_bytes.len() < 4 {
        return Err(crate::Error::Tls("ServerHello too short to peek version".into()));
    }
    let body_len = read_u24(msg_bytes, 1)? as usize;
    if msg_bytes.len() < 4 + body_len {
        return Err(crate::Error::Tls("ServerHello body truncated".into()));
    }
    let body = &msg_bytes[4..4 + body_len];
    match decode_server_hello(body)? {
        HandshakeMessage::ServerHello(sh) => Ok(sh.version),
        _ => Err(crate::Error::Tls("expected ServerHello".into())),
    }
}
```

- [ ] **Step 9: 更新 tls13/handshake.rs 中 ServerHello 处理，使用 Option 字段**

```rust
// 在 handle_message 的 ServerHello 分支，将 sh.x25519_public 改为解包 Option：
let x25519_pub = sh.x25519_public.ok_or_else(|| {
    crate::Error::Tls("TLS 1.3 ServerHello missing key_share".into())
})?;
let shared = x25519::shared_secret(&self.x25519_secret, &x25519_pub);
```

- [ ] **Step 10: 验证测试通过**

```bash
cargo test client_hello_offers_tls12 client_hello_supported_versions decode_tls12 -- --nocapture 2>&1 | grep -E "ok|FAILED"
```

Expected: 3 tests pass。

- [ ] **Step 11: 运行全量测试**

```bash
cargo test 2>&1 | tail -5
```

Expected: `test result: ok. N passed; 0 failed`

- [ ] **Step 12: Commit**

```bash
git add src/core/net/tls/
git commit -m "feat(tls): ClientHello 同时宣告 TLS 1.2，支持 TLS 1.2 ServerHello 解析"
```

---

## Task 3: TLS 1.2 key schedule（PRF、master_secret、key_block）

**Files:**
- Create: `src/core/net/tls/tls12/mod.rs`
- Create: `src/core/net/tls/tls12/key_schedule.rs`
- Create: `tests/core/net/tls/tls12/mod.rs`
- Create: `tests/core/net/tls/tls12/key_schedule_test.rs`
- Modify: `tests/core/net/tls/mod.rs`

- [ ] **Step 1: 创建 tls12/mod.rs（仅声明子模块）**

```rust
// src/core/net/tls/tls12/mod.rs
pub mod handshake;
pub mod key_schedule;
pub mod record;
```

在 `src/core/net/tls/mod.rs` 添加：

```rust
pub mod tls12;
```

- [ ] **Step 2: 写失败测试**

创建 `tests/core/net/tls/tls12/mod.rs`：

```rust
pub mod key_schedule_test;
```

创建 `tests/core/net/tls/tls12/key_schedule_test.rs`：

```rust
use viv::core::net::tls::tls12::key_schedule::{prf, master_secret, derive_key_block};

#[test]
fn prf_output_length_matches_request() {
    let secret = b"test secret";
    let seed = b"test seed";
    let mut out16 = [0u8; 16];
    let mut out48 = [0u8; 48];
    prf(secret, b"label", seed, &mut out16);
    prf(secret, b"label", seed, &mut out48);
    // If we got here without panic, output lengths are correct.
    // Verify out16 is a prefix of out48 (P_hash is sequential).
    assert_eq!(&out16[..], &out48[..16]);
}

#[test]
fn prf_is_deterministic() {
    let secret = b"secret";
    let seed = b"seed";
    let mut a = [0u8; 32];
    let mut b = [0u8; 32];
    prf(secret, b"label", seed, &mut a);
    prf(secret, b"label", seed, &mut b);
    assert_eq!(a, b);
}

#[test]
fn prf_different_labels_produce_different_output() {
    let secret = b"secret";
    let seed = b"seed";
    let mut a = [0u8; 32];
    let mut b = [0u8; 32];
    prf(secret, b"label1", seed, &mut a);
    prf(secret, b"label2", seed, &mut b);
    assert_ne!(a, b);
}

#[test]
fn master_secret_is_48_bytes() {
    let pre_master = [0x01u8; 32];
    let client_random = [0x02u8; 32];
    let server_random = [0x03u8; 32];
    let ms = master_secret(&pre_master, &client_random, &server_random);
    assert_eq!(ms.len(), 48);
}

#[test]
fn key_block_has_correct_sizes() {
    let ms = [0u8; 48];
    let cr = [0u8; 32];
    let sr = [0u8; 32];
    let kb = derive_key_block(&ms, &sr, &cr);
    // Smoke-check field sizes via usage
    assert_eq!(kb.client_write_key.len(), 16);
    assert_eq!(kb.server_write_key.len(), 16);
    assert_eq!(kb.client_write_iv.len(), 4);
    assert_eq!(kb.server_write_iv.len(), 4);
}

#[test]
fn key_block_differs_for_different_randoms() {
    let ms = [0u8; 48];
    let cr1 = [0x01u8; 32];
    let cr2 = [0x02u8; 32];
    let sr = [0u8; 32];
    let kb1 = derive_key_block(&ms, &sr, &cr1);
    let kb2 = derive_key_block(&ms, &sr, &cr2);
    assert_ne!(kb1.client_write_key, kb2.client_write_key);
}
```

在 `tests/core/net/tls/mod.rs` 追加：

```rust
pub mod tls12;
```

- [ ] **Step 3: 验证测试无法编译（模块未实现）**

```bash
cargo test --test core_tests tls12::key_schedule_test 2>&1 | grep "^error"
```

Expected: error，模块不存在。

- [ ] **Step 4: 实现 tls12/key_schedule.rs**

```rust
// src/core/net/tls/tls12/key_schedule.rs
use crate::core::net::tls::crypto::sha256::hmac_sha256;

/// P_hash(secret, seed) using HMAC-SHA256 — RFC 5246 §5.
/// Fills `out` with as many bytes as needed.
pub fn p_hash(secret: &[u8], seed: &[u8], out: &mut [u8]) {
    // A(0) = seed; A(i) = HMAC(secret, A(i-1))
    let mut a = hmac_sha256(secret, seed); // A(1)
    let mut offset = 0;
    while offset < out.len() {
        // HMAC(secret, A(i) || seed)
        let mut input = Vec::with_capacity(32 + seed.len());
        input.extend_from_slice(&a);
        input.extend_from_slice(seed);
        let block = hmac_sha256(secret, &input);
        let n = (out.len() - offset).min(32);
        out[offset..offset + n].copy_from_slice(&block[..n]);
        offset += n;
        if offset < out.len() {
            a = hmac_sha256(secret, &a); // A(i+1)
        }
    }
}

/// PRF(secret, label, seed) = P_SHA256(secret, label || seed) — RFC 5246 §5.
pub fn prf(secret: &[u8], label: &[u8], seed: &[u8], out: &mut [u8]) {
    let mut label_seed = Vec::with_capacity(label.len() + seed.len());
    label_seed.extend_from_slice(label);
    label_seed.extend_from_slice(seed);
    p_hash(secret, &label_seed, out);
}

/// master_secret = PRF(pre_master_secret, "master secret", ClientRandom || ServerRandom)
pub fn master_secret(pre_master: &[u8], client_random: &[u8; 32], server_random: &[u8; 32]) -> [u8; 48] {
    let mut seed = [0u8; 64];
    seed[..32].copy_from_slice(client_random);
    seed[32..].copy_from_slice(server_random);
    let mut out = [0u8; 48];
    prf(pre_master, b"master secret", &seed, &mut out);
    out
}

/// key_block = PRF(master, "key expansion", ServerRandom || ClientRandom)
/// Layout: client_write_key(16) | server_write_key(16) | client_write_IV(4) | server_write_IV(4)
pub struct KeyBlock {
    pub client_write_key: [u8; 16],
    pub server_write_key: [u8; 16],
    pub client_write_iv: [u8; 4],
    pub server_write_iv: [u8; 4],
}

pub fn derive_key_block(
    master: &[u8; 48],
    server_random: &[u8; 32],
    client_random: &[u8; 32],
) -> KeyBlock {
    let mut seed = [0u8; 64];
    seed[..32].copy_from_slice(server_random); // server first for key expansion
    seed[32..].copy_from_slice(client_random);
    let mut kb = [0u8; 40]; // 16+16+4+4
    prf(master, b"key expansion", &seed, &mut kb);
    let mut client_write_key = [0u8; 16];
    let mut server_write_key = [0u8; 16];
    let mut client_write_iv = [0u8; 4];
    let mut server_write_iv = [0u8; 4];
    client_write_key.copy_from_slice(&kb[0..16]);
    server_write_key.copy_from_slice(&kb[16..32]);
    client_write_iv.copy_from_slice(&kb[32..36]);
    server_write_iv.copy_from_slice(&kb[36..40]);
    KeyBlock { client_write_key, server_write_key, client_write_iv, server_write_iv }
}

/// Finished verify data = PRF(master, label, transcript_hash)[..12]
pub fn finished_verify_data(master: &[u8; 48], label: &[u8], transcript_hash: &[u8; 32]) -> [u8; 12] {
    let mut out = [0u8; 12];
    prf(master, label, transcript_hash, &mut out);
    out
}
```

- [ ] **Step 5: 验证测试通过**

```bash
cargo test --test core_tests tls12::key_schedule_test 2>&1 | tail -5
```

Expected: 6 tests pass。

- [ ] **Step 6: Commit**

```bash
git add src/core/net/tls/tls12/ tests/core/net/tls/tls12/ tests/core/net/tls/mod.rs
git commit -m "feat(tls12): TLS 1.2 key schedule — PRF/master_secret/key_block"
```

---

## Task 4: P-256 点序列化（to_uncompressed）

**Files:**
- Modify: `src/core/net/tls/p256.rs`
- Test: `tests/core/net/tls/p256_test.rs`

- [ ] **Step 1: 写失败测试**

在 `tests/core/net/tls/p256_test.rs` 末尾追加：

```rust
#[test]
fn generator_to_uncompressed_starts_with_04() {
    use viv::core::net::tls::p256::Point;
    let g = Point::generator();
    let bytes = g.to_uncompressed().expect("generator is not infinity");
    assert_eq!(bytes[0], 0x04, "uncompressed point must start with 0x04");
    assert_eq!(bytes.len(), 65);
}

#[test]
fn scalar_mul_to_uncompressed_roundtrip() {
    use viv::core::net::tls::p256::Point;
    let scalar = [2u8; 32];
    let pt = Point::generator().scalar_mul(&scalar);
    let bytes = pt.to_uncompressed().expect("non-infinity point");
    let pt2 = Point::from_uncompressed(&bytes).expect("valid uncompressed point");
    // Both points have the same affine x
    assert_eq!(pt.affine_x_bytes(), pt2.affine_x_bytes());
}
```

- [ ] **Step 2: 验证测试失败**

```bash
cargo test generator_to_uncompressed scalar_mul_to_uncompressed 2>&1 | grep "FAILED\|error\[E"
```

Expected: 编译错误（方法不存在）。

- [ ] **Step 3: 实现 to_uncompressed**

在 `src/core/net/tls/p256.rs` 的 `affine_x_bytes` 方法后面添加：

```rust
/// Serialize to uncompressed SEC1 format: 0x04 || x(32) || y(32).
/// Returns None for the point at infinity.
pub fn to_uncompressed(&self) -> Option<[u8; 65]> {
    if self.is_infinity() {
        return None;
    }
    // Convert Jacobian to affine: (X/Z², Y/Z³)
    let z2 = self.z.square();
    let z3 = z2.mul(&self.z);
    let z2_inv = z2.invert()?;
    let z3_inv = z3.invert()?;
    let ax = self.x.mul(&z2_inv).to_bytes_be();
    let ay = self.y.mul(&z3_inv).to_bytes_be();
    let mut out = [0u8; 65];
    out[0] = 0x04;
    out[1..33].copy_from_slice(&ax);
    out[33..65].copy_from_slice(&ay);
    Some(out)
}
```

- [ ] **Step 4: 验证测试通过**

```bash
cargo test generator_to_uncompressed scalar_mul_to_uncompressed 2>&1 | tail -3
```

Expected: 2 tests pass。

- [ ] **Step 5: Commit**

```bash
git add src/core/net/tls/p256.rs tests/core/net/tls/p256_test.rs
git commit -m "feat(p256): 添加 to_uncompressed() 序列化方法"
```

---

## Task 5: TLS 1.2 record layer（显式 IV）

**Files:**
- Create: `src/core/net/tls/tls12/record.rs`
- Create: `tests/core/net/tls/tls12/record_test.rs`
- Modify: `tests/core/net/tls/tls12/mod.rs`

- [ ] **Step 1: 写失败测试**

在 `tests/core/net/tls/tls12/mod.rs` 添加 `pub mod record_test;`。

创建 `tests/core/net/tls/tls12/record_test.rs`：

```rust
use viv::core::net::tls::tls12::record::Tls12RecordLayer;
use viv::core::net::tls::tls12::key_schedule::KeyBlock;
use viv::core::net::tls::codec::APPLICATION_DATA;

fn test_key_block() -> KeyBlock {
    KeyBlock {
        client_write_key: [0x01u8; 16],
        server_write_key: [0x02u8; 16],
        client_write_iv:  [0x03u8; 4],
        server_write_iv:  [0x04u8; 4],
    }
}

#[test]
fn encrypt_then_decrypt_roundtrip() {
    let kb = test_key_block();
    let mut client = Tls12RecordLayer::new();
    let mut server = Tls12RecordLayer::new();
    client.install_client_keys(&kb);
    server.install_server_keys(&kb);

    let plaintext = b"hello tls12";
    let mut record = Vec::new();
    client.write_encrypted(APPLICATION_DATA, plaintext, &mut record).unwrap();

    let (ct, payload, consumed) = server.read_record(&record).unwrap();
    assert_eq!(consumed, record.len());
    assert_eq!(ct, APPLICATION_DATA);
    assert_eq!(payload, plaintext);
}

#[test]
fn encrypted_record_contains_explicit_iv() {
    let kb = test_key_block();
    let mut client = Tls12RecordLayer::new();
    client.install_client_keys(&kb);

    let plaintext = b"test";
    let mut record = Vec::new();
    client.write_encrypted(APPLICATION_DATA, plaintext, &mut record).unwrap();

    // TLS 1.2 record: header(5) + explicit_iv(8) + ciphertext + tag(16)
    assert!(record.len() >= 5 + 8 + plaintext.len() + 16);
}

#[test]
fn sequence_number_increments_produce_distinct_ciphertexts() {
    let kb = test_key_block();
    let mut client = Tls12RecordLayer::new();
    client.install_client_keys(&kb);

    let plaintext = b"same message";
    let mut r1 = Vec::new();
    let mut r2 = Vec::new();
    client.write_encrypted(APPLICATION_DATA, plaintext, &mut r1).unwrap();
    client.write_encrypted(APPLICATION_DATA, plaintext, &mut r2).unwrap();
    // Different nonces → different ciphertexts
    assert_ne!(r1, r2);
}
```

- [ ] **Step 2: 验证测试编译失败**

```bash
cargo test --test core_tests tls12::record_test 2>&1 | grep "^error"
```

Expected: error（模块/结构不存在）。

- [ ] **Step 3: 实现 tls12/record.rs**

```rust
// src/core/net/tls/tls12/record.rs
use crate::core::net::tls::crypto::aes_gcm::Aes128Gcm;
use crate::core::net::tls::tls12::key_schedule::KeyBlock;

struct Encrypter {
    cipher: Aes128Gcm,
    implicit_iv: [u8; 4],
    seq: u64,
}

impl Encrypter {
    fn new(key: [u8; 16], implicit_iv: [u8; 4]) -> Self {
        Self { cipher: Aes128Gcm::new(&key), implicit_iv, seq: 0 }
    }

    fn encrypt(&mut self, content_type: u8, payload: &[u8], out: &mut Vec<u8>) -> crate::Result<()> {
        let explicit_iv = self.seq.to_be_bytes();
        let mut nonce = [0u8; 12];
        nonce[..4].copy_from_slice(&self.implicit_iv);
        nonce[4..].copy_from_slice(&explicit_iv);

        // AAD = seq(8) | type(1) | version(2=0x0303) | plaintext_len(2)
        let plain_len = payload.len() as u16;
        let mut aad = [0u8; 13];
        aad[..8].copy_from_slice(&explicit_iv);
        aad[8] = content_type;
        aad[9] = 0x03;
        aad[10] = 0x03;
        aad[11] = (plain_len >> 8) as u8;
        aad[12] = plain_len as u8;

        // Record: header(5) + explicit_iv(8) + ciphertext(n) + tag(16)
        let ct_len = 8 + payload.len() + 16;
        out.push(content_type);
        out.push(0x03);
        out.push(0x03);
        out.push((ct_len >> 8) as u8);
        out.push(ct_len as u8);
        out.extend_from_slice(&explicit_iv);

        let ct_start = out.len();
        out.resize(ct_start + payload.len() + 16, 0);
        self.cipher.encrypt(&nonce, &aad, payload, &mut out[ct_start..])?;

        self.seq += 1;
        Ok(())
    }
}

struct Decrypter {
    cipher: Aes128Gcm,
    implicit_iv: [u8; 4],
    seq: u64,
}

impl Decrypter {
    fn new(key: [u8; 16], implicit_iv: [u8; 4]) -> Self {
        Self { cipher: Aes128Gcm::new(&key), implicit_iv, seq: 0 }
    }

    fn decrypt(&mut self, content_type: u8, payload: &[u8]) -> crate::Result<Vec<u8>> {
        if payload.len() < 8 + 16 {
            return Err(crate::Error::Tls("TLS 1.2 record too short to decrypt".into()));
        }
        let mut explicit_iv = [0u8; 8];
        explicit_iv.copy_from_slice(&payload[..8]);
        let ciphertext_and_tag = &payload[8..];

        let mut nonce = [0u8; 12];
        nonce[..4].copy_from_slice(&self.implicit_iv);
        nonce[4..].copy_from_slice(&explicit_iv);

        let plain_len = (ciphertext_and_tag.len() - 16) as u16;
        let mut aad = [0u8; 13];
        aad[..8].copy_from_slice(&self.seq.to_be_bytes());
        aad[8] = content_type;
        aad[9] = 0x03;
        aad[10] = 0x03;
        aad[11] = (plain_len >> 8) as u8;
        aad[12] = plain_len as u8;

        let mut plaintext = vec![0u8; ciphertext_and_tag.len() - 16];
        self.cipher.decrypt(&nonce, &aad, ciphertext_and_tag, &mut plaintext)?;

        self.seq += 1;
        Ok(plaintext)
    }
}

pub struct Tls12RecordLayer {
    encrypter: Option<Encrypter>,
    decrypter: Option<Decrypter>,
}

impl Tls12RecordLayer {
    pub fn new() -> Self {
        Self { encrypter: None, decrypter: None }
    }

    /// Install client-side keys (client writes, server reads).
    pub fn install_client_keys(&mut self, kb: &KeyBlock) {
        self.encrypter = Some(Encrypter::new(kb.client_write_key, kb.client_write_iv));
        self.decrypter = Some(Decrypter::new(kb.server_write_key, kb.server_write_iv));
    }

    /// Install server-side keys (server writes, client reads).
    pub fn install_server_keys(&mut self, kb: &KeyBlock) {
        self.encrypter = Some(Encrypter::new(kb.server_write_key, kb.server_write_iv));
        self.decrypter = Some(Decrypter::new(kb.client_write_key, kb.client_write_iv));
    }

    pub fn write_plaintext(&self, content_type: u8, payload: &[u8], out: &mut Vec<u8>) {
        out.push(content_type);
        out.push(0x03);
        out.push(0x03);
        out.push((payload.len() >> 8) as u8);
        out.push(payload.len() as u8);
        out.extend_from_slice(payload);
    }

    pub fn write_encrypted(&mut self, content_type: u8, payload: &[u8], out: &mut Vec<u8>) -> crate::Result<()> {
        let enc = self.encrypter.as_mut().ok_or_else(|| {
            crate::Error::Tls("write_encrypted called before keys installed".into())
        })?;
        enc.encrypt(content_type, payload, out)
    }

    /// Read one TLS record from `data`. Returns (content_type, plaintext, bytes_consumed).
    /// If keys are installed and record is APPLICATION_DATA, decrypts automatically.
    pub fn read_record(&mut self, data: &[u8]) -> crate::Result<(u8, Vec<u8>, usize)> {
        use crate::core::net::tls::codec::APPLICATION_DATA;
        if data.len() < 5 {
            return Err(crate::Error::Tls("record too short".into()));
        }
        let content_type = data[0];
        let length = ((data[3] as usize) << 8) | data[4] as usize;
        if data.len() < 5 + length {
            return Err(crate::Error::Tls("record body truncated".into()));
        }
        let consumed = 5 + length;
        let payload = &data[5..consumed];

        if content_type == APPLICATION_DATA {
            if let Some(dec) = &mut self.decrypter {
                let plaintext = dec.decrypt(content_type, payload)?;
                return Ok((content_type, plaintext, consumed));
            }
        }
        Ok((content_type, payload.to_vec(), consumed))
    }
}
```

- [ ] **Step 4: 验证测试通过**

```bash
cargo test --test core_tests tls12::record_test 2>&1 | tail -5
```

Expected: 3 tests pass。

- [ ] **Step 5: Commit**

```bash
git add src/core/net/tls/tls12/record.rs tests/core/net/tls/tls12/record_test.rs tests/core/net/tls/tls12/mod.rs
git commit -m "feat(tls12): TLS 1.2 record layer — 显式 IV AES-128-GCM"
```

---

## Task 6: TLS 1.2 codec — ServerKeyExchange/ClientKeyExchange

**Files:**
- Modify: `src/core/net/tls/codec.rs`
- Test: `tests/core/net/tls/codec_test.rs`

- [ ] **Step 1: 写失败测试**

在 `tests/core/net/tls/codec_test.rs` 追加：

```rust
#[test]
fn decode_server_key_exchange_extracts_p256_pubkey() {
    use viv::core::net::tls::codec::{decode_handshake, HandshakeMessage};

    // Build a ServerKeyExchange with P-256 ECDHE (curve_type=3, named_curve=0x0017)
    let fake_pubkey = [0x04u8; 65]; // fake uncompressed P-256 point
    let fake_sig = [0xABu8; 64];

    let mut body = Vec::new();
    body.push(3);               // curve_type = named_curve
    body.extend_from_slice(&[0x00, 0x17]); // secp256r1
    body.push(65);              // pubkey length
    body.extend_from_slice(&fake_pubkey);
    // sig_alg: rsa_pkcs1_sha256 (0x0401)
    body.extend_from_slice(&[0x04, 0x01]);
    body.extend_from_slice(&[0x00, 64u8]); // sig length
    body.extend_from_slice(&fake_sig);

    let mut msg = vec![0x0C]; // SERVER_KEY_EXCHANGE
    let len = body.len() as u32;
    msg.push((len >> 16) as u8);
    msg.push((len >> 8) as u8);
    msg.push(len as u8);
    msg.extend_from_slice(&body);

    let decoded = decode_handshake(&msg).unwrap();
    if let HandshakeMessage::ServerKeyExchange(ske) = decoded {
        assert_eq!(ske.named_curve, 0x0017);
        assert_eq!(ske.public_key.len(), 65);
        assert_eq!(&ske.public_key, &fake_pubkey);
    } else {
        panic!("expected ServerKeyExchange");
    }
}

#[test]
fn encode_client_key_exchange_format() {
    use viv::core::net::tls::codec::encode_client_key_exchange;
    let pubkey = [0x04u8; 65];
    let mut out = Vec::new();
    encode_client_key_exchange(&pubkey, &mut out);
    // type(1) = 0x10 + len(3) + pubkey_len(1) + pubkey(65)
    assert_eq!(out[0], 0x10); // CLIENT_KEY_EXCHANGE
    assert_eq!(out[4], 65);   // pubkey length byte
    assert_eq!(&out[5..70], &pubkey[..]);
}
```

- [ ] **Step 2: 验证测试失败**

```bash
cargo test decode_server_key_exchange encode_client_key_exchange 2>&1 | grep "FAILED\|error\[E"
```

Expected: 编译错误（类型/函数不存在）。

- [ ] **Step 3: 添加常量和 ServerKeyExchange 类型到 codec.rs**

在常量区追加：

```rust
pub const SERVER_KEY_EXCHANGE: u8 = 0x0c;
pub const SERVER_HELLO_DONE: u8 = 0x0e;
pub const CLIENT_KEY_EXCHANGE: u8 = 0x10;
```

在 `HandshakeMessage` enum 追加：

```rust
pub enum HandshakeMessage {
    ServerHello(ServerHello),
    EncryptedExtensions,
    Certificate(Vec<Vec<u8>>),
    CertificateVerify { scheme: u16, signature: Vec<u8> },
    Finished { verify_data: [u8; 32] },
    // TLS 1.2 specific:
    ServerKeyExchange(ServerKeyExchange),
    ServerHelloDone,
}

pub struct ServerKeyExchange {
    pub named_curve: u16,
    pub public_key: Vec<u8>,   // uncompressed P-256: 65 bytes
    pub sig_scheme: u16,
    pub signature: Vec<u8>,
    pub signed_data: Vec<u8>,  // bytes that were signed (for verification)
}
```

- [ ] **Step 4: 实现 decode_server_key_exchange**

```rust
fn decode_server_key_exchange(body: &[u8]) -> crate::Result<HandshakeMessage> {
    if body.len() < 4 {
        return Err(crate::Error::Tls("ServerKeyExchange too short".into()));
    }
    let curve_type = body[0];
    if curve_type != 3 {
        return Err(crate::Error::Tls(format!(
            "unsupported SKE curve type: {}", curve_type
        )));
    }
    let named_curve = read_u16(body, 1)?;
    let pubkey_len = body[3] as usize;
    if body.len() < 4 + pubkey_len + 4 {
        return Err(crate::Error::Tls("ServerKeyExchange pubkey truncated".into()));
    }
    let public_key = body[4..4 + pubkey_len].to_vec();
    let signed_data = body[..4 + pubkey_len].to_vec();

    let sp = 4 + pubkey_len;
    let sig_scheme = read_u16(body, sp)?;
    let sig_len = read_u16(body, sp + 2)? as usize;
    if body.len() < sp + 4 + sig_len {
        return Err(crate::Error::Tls("ServerKeyExchange sig truncated".into()));
    }
    let signature = body[sp + 4..sp + 4 + sig_len].to_vec();

    Ok(HandshakeMessage::ServerKeyExchange(ServerKeyExchange {
        named_curve,
        public_key,
        sig_scheme,
        signature,
        signed_data,
    }))
}
```

- [ ] **Step 5: 实现 encode_client_key_exchange**

```rust
/// Encode a TLS 1.2 ClientKeyExchange message (ECDHE public key).
pub fn encode_client_key_exchange(pubkey: &[u8; 65], out: &mut Vec<u8>) {
    // type(1) + len(3) + pubkey_len(1) + pubkey(65)
    out.push(CLIENT_KEY_EXCHANGE);
    push_u24(out, 66); // 1 + 65
    out.push(65);
    out.extend_from_slice(pubkey);
}
```

- [ ] **Step 6: 更新 decode_handshake 分发新消息类型**

在 `decode_handshake` 的 match 中追加：

```rust
SERVER_KEY_EXCHANGE => decode_server_key_exchange(body),
SERVER_HELLO_DONE => Ok(HandshakeMessage::ServerHelloDone),
```

- [ ] **Step 7: 验证测试通过**

```bash
cargo test decode_server_key_exchange encode_client_key_exchange 2>&1 | tail -3
```

Expected: 2 tests pass。

- [ ] **Step 8: 全量回归**

```bash
cargo test 2>&1 | tail -5
```

Expected: `test result: ok. N passed; 0 failed`

- [ ] **Step 9: Commit**

```bash
git add src/core/net/tls/codec.rs tests/core/net/tls/codec_test.rs
git commit -m "feat(tls12): codec — ServerKeyExchange/ServerHelloDone/ClientKeyExchange"
```

---

## Task 7: TLS 1.2 握手状态机（Tls12Handshake）

**Files:**
- Create: `src/core/net/tls/tls12/handshake.rs`
- Create: `tests/core/net/tls/tls12/handshake_test.rs`
- Modify: `tests/core/net/tls/tls12/mod.rs`

- [ ] **Step 1: 写失败测试**

在 `tests/core/net/tls/tls12/mod.rs` 追加 `pub mod handshake_test;`。

创建 `tests/core/net/tls/tls12/handshake_test.rs`：

```rust
use viv::core::net::tls::tls12::handshake::{Tls12Handshake, Tls12HandshakeResult};
use viv::core::net::tls::tls12::record::Tls12RecordLayer;
use viv::core::net::tls::codec::{
    decode_handshake, HandshakeMessage, ServerKeyExchange, encode_client_key_exchange,
};
use viv::core::net::tls::p256::Point;

fn make_handshake() -> Tls12Handshake {
    let client_random = [0x01u8; 32];
    let server_random = [0x02u8; 32];
    Tls12Handshake::new(&client_random, &server_random, 0xC02B).unwrap()
}

#[test]
fn certificate_transitions_to_expect_ske() {
    let mut hs = make_handshake();
    let mut record = Tls12RecordLayer::new();

    // Minimal TLS 1.2 Certificate message (empty list for testing)
    let cert_body = vec![0x00, 0x00, 0x03, 0x00, 0x00, 0x00]; // context + empty list
    let mut msg = vec![0x0B]; // CERTIFICATE
    let len = cert_body.len() as u32;
    msg.push((len >> 16) as u8);
    msg.push((len >> 8) as u8);
    msg.push(len as u8);
    msg.extend_from_slice(&cert_body);

    let result = hs.handle_message(&msg, &mut record).unwrap();
    assert!(matches!(result, Tls12HandshakeResult::Continue));
}

#[test]
fn server_hello_done_returns_send_to_server() {
    let mut hs = make_handshake();
    let mut record = Tls12RecordLayer::new();

    // Feed Certificate (empty)
    {
        let cert_body = vec![0x00, 0x00, 0x03, 0x00, 0x00, 0x00];
        let mut msg = vec![0x0B];
        let len = cert_body.len() as u32;
        msg.push((len >> 16) as u8);
        msg.push((len >> 8) as u8);
        msg.push(len as u8);
        msg.extend_from_slice(&cert_body);
        hs.handle_message(&msg, &mut record).unwrap();
    }

    // Feed ServerKeyExchange with valid P-256 public key
    let server_p256_pub = Point::generator().scalar_mul(&[3u8; 32])
        .to_uncompressed().unwrap();
    let ske = build_ske_msg(&server_p256_pub, 0xC02B);
    hs.handle_message(&ske, &mut record).unwrap();

    // Feed ServerHelloDone
    let shd = vec![0x0E, 0x00, 0x00, 0x00]; // SERVER_HELLO_DONE + len=0
    let result = hs.handle_message(&shd, &mut record).unwrap();
    assert!(
        matches!(result, Tls12HandshakeResult::SendToServer(_)),
        "Expected SendToServer after ServerHelloDone"
    );
}

fn build_ske_msg(pubkey: &[u8; 65], _cipher_suite: u16) -> Vec<u8> {
    // Minimal SKE: curve_type=3, secp256r1=0x0017, pubkey, fake sig
    let fake_sig = [0u8; 64];
    let mut body = Vec::new();
    body.push(3);
    body.extend_from_slice(&[0x00, 0x17]);
    body.push(65);
    body.extend_from_slice(pubkey);
    body.extend_from_slice(&[0x04, 0x01]); // rsa_pkcs1_sha256
    body.extend_from_slice(&[0x00, 64]);
    body.extend_from_slice(&fake_sig);

    let mut msg = vec![0x0C]; // SERVER_KEY_EXCHANGE
    let len = body.len() as u32;
    msg.push((len >> 16) as u8);
    msg.push((len >> 8) as u8);
    msg.push(len as u8);
    msg.extend_from_slice(&body);
    msg
}
```

- [ ] **Step 2: 验证测试编译失败**

```bash
cargo test --test core_tests tls12::handshake_test 2>&1 | grep "^error"
```

Expected: error（类型不存在）。

- [ ] **Step 3: 实现 tls12/handshake.rs**

```rust
// src/core/net/tls/tls12/handshake.rs
use crate::core::net::tls::codec::{self, HandshakeMessage, encode_client_key_exchange};
use crate::core::net::tls::crypto::sha256::Sha256;
use crate::core::net::tls::p256::Point;
use crate::core::net::tls::tls12::key_schedule::{
    derive_key_block, finished_verify_data, master_secret, KeyBlock,
};
use crate::core::net::tls::tls12::record::Tls12RecordLayer;
use crate::core::net::tls::crypto;

#[derive(Debug, PartialEq)]
enum State {
    ExpectCertificate,
    ExpectServerKeyExchange,
    ExpectServerHelloDone,
    ExpectServerChangeCipherSpec,
    ExpectServerFinished,
    Complete,
}

pub enum Tls12HandshakeResult {
    Continue,
    /// Bytes to write to the TCP stream (ClientKeyExchange + CCS + Finished).
    SendToServer(Vec<u8>),
    Complete,
}

pub struct Tls12Handshake {
    state: State,
    transcript: Sha256,
    client_random: [u8; 32],
    server_random: [u8; 32],
    cipher_suite: u16,
    p256_secret: [u8; 32],
    p256_public: [u8; 65],
    master_secret: Option<[u8; 48]>,
}

impl Tls12Handshake {
    /// Create a new TLS 1.2 handshake. `client_random` and `server_random` come
    /// from ClientHello and ServerHello respectively.
    pub fn new(client_random: &[u8; 32], server_random: &[u8; 32], cipher_suite: u16) -> crate::Result<Self> {
        // Generate ephemeral P-256 key pair
        let mut secret = [0u8; 32];
        crypto::getrandom(&mut secret)?;
        let public_point = Point::generator().scalar_mul(&secret);
        let p256_public = public_point
            .to_uncompressed()
            .ok_or_else(|| crate::Error::Tls("P-256 keygen produced infinity".into()))?;
        Ok(Self {
            state: State::ExpectCertificate,
            transcript: Sha256::new(),
            client_random: *client_random,
            server_random: *server_random,
            cipher_suite,
            p256_secret: secret,
            p256_public,
            master_secret: None,
        })
    }

    /// Process one handshake message. Returns action for the caller.
    pub fn handle_message(
        &mut self,
        msg_bytes: &[u8],
        record: &mut Tls12RecordLayer,
    ) -> crate::Result<Tls12HandshakeResult> {
        let msg = codec::decode_handshake(msg_bytes)?;

        match (&self.state, msg) {
            (State::ExpectCertificate, HandshakeMessage::Certificate(_certs)) => {
                self.transcript.update(msg_bytes);
                // Certificate verification is out of scope for this implementation.
                self.state = State::ExpectServerKeyExchange;
                Ok(Tls12HandshakeResult::Continue)
            }

            (State::ExpectServerKeyExchange, HandshakeMessage::ServerKeyExchange(ske)) => {
                self.transcript.update(msg_bytes);
                // Validate it's P-256
                if ske.named_curve != 0x0017 {
                    return Err(crate::Error::Tls(format!(
                        "unsupported TLS 1.2 named curve: 0x{:04x}", ske.named_curve
                    )));
                }
                if ske.public_key.len() != 65 {
                    return Err(crate::Error::Tls("SKE public key is not 65 bytes".into()));
                }
                let mut pk_bytes = [0u8; 65];
                pk_bytes.copy_from_slice(&ske.public_key);
                let server_point = Point::from_uncompressed(&pk_bytes)?;
                // Signature verification: omitted (future work for security hardening).
                // Compute shared secret: x-coordinate of (our_secret × server_point)
                let shared_point = server_point.scalar_mul(&self.p256_secret);
                let shared_x = shared_point.affine_x_bytes()
                    .ok_or_else(|| crate::Error::Tls("ECDH produced infinity".into()))?;
                // Derive master_secret
                let ms = master_secret(&shared_x, &self.client_random, &self.server_random);
                self.master_secret = Some(ms);
                self.state = State::ExpectServerHelloDone;
                Ok(Tls12HandshakeResult::Continue)
            }

            (State::ExpectServerHelloDone, HandshakeMessage::ServerHelloDone) => {
                self.transcript.update(msg_bytes);
                let ms = self.master_secret.ok_or_else(|| {
                    crate::Error::Tls("master_secret missing at ServerHelloDone".into())
                })?;
                let kb = derive_key_block(&ms, &self.server_random, &self.client_random);

                // Build ClientKeyExchange message
                let mut cke_msg = Vec::new();
                encode_client_key_exchange(&self.p256_public, &mut cke_msg);
                self.transcript.update(&cke_msg);

                // Install client-side keys on record layer
                record.install_client_keys(&kb);

                // Build ChangeCipherSpec (plaintext)
                let mut ccs = Vec::new();
                crate::core::net::tls::codec::encode_change_cipher_spec(&mut ccs);

                // Build encrypted Finished
                let transcript_hash = self.transcript.clone().finish();
                let verify_data = finished_verify_data(&ms, b"client finished", &transcript_hash);
                let mut finished_msg = Vec::new();
                finished_msg.push(crate::core::net::tls::codec::FINISHED);
                finished_msg.push(0);
                finished_msg.push(0);
                finished_msg.push(12);
                finished_msg.extend_from_slice(&verify_data);
                self.transcript.update(&finished_msg);

                let mut enc_finished = Vec::new();
                record.write_encrypted(
                    crate::core::net::tls::codec::HANDSHAKE,
                    &finished_msg,
                    &mut enc_finished,
                )?;

                // Combine: CKE (plaintext record) + CCS + encrypted Finished
                let mut to_send = Vec::new();
                // CKE as plaintext record
                let mut cke_record = Vec::new();
                record.write_plaintext(crate::core::net::tls::codec::HANDSHAKE, &cke_msg, &mut cke_record);
                to_send.extend_from_slice(&cke_record);
                to_send.extend_from_slice(&ccs);
                to_send.extend_from_slice(&enc_finished);

                self.state = State::ExpectServerChangeCipherSpec;
                Ok(Tls12HandshakeResult::SendToServer(to_send))
            }

            (State::ExpectServerChangeCipherSpec, HandshakeMessage::ServerHelloDone) => {
                // ChangeCipherSpec is not a handshake message — handled externally.
                // This branch shouldn't be reached; CCS is intercepted in the connect loop.
                Err(crate::Error::Tls("unexpected message while waiting for server CCS".into()))
            }

            (State::ExpectServerFinished, HandshakeMessage::Finished { verify_data }) => {
                let ms = self.master_secret.ok_or_else(|| {
                    crate::Error::Tls("master_secret missing at server Finished".into())
                })?;
                let transcript_hash = self.transcript.clone().finish();
                let expected = finished_verify_data(&ms, b"server finished", &transcript_hash);

                // Constant-time comparison (12 bytes)
                let mut diff = 0u8;
                for i in 0..12 {
                    diff |= verify_data[i] ^ expected[i];
                }
                if diff != 0 {
                    return Err(crate::Error::Tls("TLS 1.2 server Finished verify failed".into()));
                }

                self.transcript.update(
                    // We don't have msg_bytes here since Finished was decrypted;
                    // in TLS 1.2 the transcript doesn't include the outer record,
                    // just the handshake message content (already added above via clone).
                    &[],
                );
                self.state = State::Complete;
                Ok(Tls12HandshakeResult::Complete)
            }

            (state, msg_type) => Err(crate::Error::Tls(format!(
                "TLS 1.2 unexpected message in state {:?}", state
            ))),
        }
    }

    /// Called when server ChangeCipherSpec is received.
    /// Installs server decrypter on the record layer.
    pub fn handle_server_ccs(&mut self, record: &mut Tls12RecordLayer) -> crate::Result<()> {
        if self.state != State::ExpectServerChangeCipherSpec {
            return Err(crate::Error::Tls("unexpected CCS".into()));
        }
        // Keys already installed (client side) — server side was already set by install_client_keys.
        self.state = State::ExpectServerFinished;
        Ok(())
    }
}
```

> **注意**：`handle_message` 对 `Finished` 的 `verify_data` 目前期望 `[u8; 32]`（TLS 1.3 格式）。TLS 1.2 的 `verify_data` 是 12 字节。需要更新 `codec.rs` 中 `Finished` 变体的 `verify_data` 类型为 `Vec<u8>`，同时更新 TLS 1.3 handshake 中的用法。

- [ ] **Step 4: 将 codec.rs 中 Finished 的 verify_data 改为 Vec<u8>**

```rust
// codec.rs — HandshakeMessage enum
Finished { verify_data: Vec<u8> },

// decode_finished 改为：
fn decode_finished(body: &[u8]) -> crate::Result<HandshakeMessage> {
    Ok(HandshakeMessage::Finished { verify_data: body.to_vec() })
}
```

在 `tls13/handshake.rs` 的 Finished 分支，改为从 Vec 中取 32 字节：

```rust
(State::ExpectFinished, HandshakeMessage::Finished { verify_data }) => {
    if verify_data.len() < 32 {
        return Err(crate::Error::Tls("TLS 1.3 Finished too short".into()));
    }
    let mut vd = [0u8; 32];
    vd.copy_from_slice(&verify_data[..32]);
    // ... 后续用 vd 替代原来的 verify_data ...
```

在 `tls12/handshake.rs` 的 Finished 分支，改为：

```rust
(State::ExpectServerFinished, HandshakeMessage::Finished { verify_data }) => {
    if verify_data.len() < 12 {
        return Err(crate::Error::Tls("TLS 1.2 Finished too short".into()));
    }
    let mut expected_verify = [0u8; 12];
    // ... 取 expected[..12] 对比 verify_data[..12] ...
```

- [ ] **Step 5: 验证测试通过**

```bash
cargo test --test core_tests tls12::handshake_test 2>&1 | tail -5
```

Expected: 2 tests pass。

- [ ] **Step 6: 全量回归**

```bash
cargo test 2>&1 | tail -5
```

Expected: `test result: ok. N passed; 0 failed`

- [ ] **Step 7: Commit**

```bash
git add src/core/net/tls/tls12/handshake.rs src/core/net/tls/codec.rs src/core/net/tls/tls13/handshake.rs tests/core/net/tls/tls12/handshake_test.rs tests/core/net/tls/tls12/mod.rs
git commit -m "feat(tls12): Tls12Handshake 状态机"
```

---

## Task 8: TlsStream 版本协商与 connect_tls12

**Files:**
- Modify: `src/core/net/tls/mod.rs`

- [ ] **Step 1: 添加 RecordLayerVariant enum**

在 `src/core/net/tls/mod.rs` 的 TlsStream 定义前添加：

```rust
use tls12::record::Tls12RecordLayer;
use tls13::record::RecordLayer;

enum RecordLayerVariant {
    Tls13(RecordLayer),
    Tls12(Tls12RecordLayer),
}

impl RecordLayerVariant {
    fn write_encrypted(&mut self, ct: u8, payload: &[u8], out: &mut Vec<u8>) -> crate::Result<()> {
        match self {
            RecordLayerVariant::Tls13(r) => r.write_encrypted(ct, payload, out),
            RecordLayerVariant::Tls12(r) => r.write_encrypted(ct, payload, out),
        }
    }

    fn read_record(&mut self, data: &[u8]) -> crate::Result<(u8, Vec<u8>, usize)> {
        match self {
            RecordLayerVariant::Tls13(r) => r.read_record(data),
            RecordLayerVariant::Tls12(r) => r.read_record(data),
        }
    }
}
```

- [ ] **Step 2: 更新 TlsStream 结构体字段**

```rust
pub struct TlsStream {
    tcp: std::net::TcpStream,
    record: RecordLayerVariant,
    read_buf: Vec<u8>,
    plaintext_buf: Vec<u8>,
}
```

- [ ] **Step 3: 更新 TlsStream::connect 解析 ServerHello 版本后分叉**

将现有 `connect` 函数重构为：

```rust
pub fn connect(host: &str, port: u16) -> crate::Result<Self> {
    let mut tcp_stream = tcp::connect(host, port)?;
    let mut init_record = RecordLayer::new();
    let mut hs = tls13::handshake::Handshake::new(host)?;
    let client_random = *hs.client_random();

    // 1. Send ClientHello
    let ch_msg = hs.encode_client_hello()?;
    let mut ch_record = Vec::new();
    init_record.write_plaintext(HANDSHAKE, &ch_msg, &mut ch_record);
    tcp_stream.write_all(&ch_record).map_err(crate::Error::Io)?;

    // 2. Read until we have the ServerHello record
    let mut read_buf = Vec::new();
    let mut tmp = [0u8; 8192];
    let (server_hello_payload, remaining_after_sh) = loop {
        // Try to consume one complete record
        if read_buf.len() >= 5 {
            let rec_len = ((read_buf[3] as usize) << 8) | (read_buf[4] as usize);
            if read_buf.len() >= 5 + rec_len {
                let (ct, payload, consumed) = init_record.read_record(&read_buf)?;
                read_buf.drain(..consumed);
                if ct == HANDSHAKE {
                    break (payload, read_buf.clone());
                }
                if ct == ALERT {
                    if payload.len() >= 2 {
                        return Err(crate::Error::Tls(format!(
                            "TLS alert: level={} desc={}", payload[0], payload[1]
                        )));
                    }
                    return Err(crate::Error::Tls("TLS alert received".into()));
                }
            }
        }
        let n = tcp_stream.read(&mut tmp).map_err(crate::Error::Io)?;
        if n == 0 {
            return Err(crate::Error::Tls("connection closed before ServerHello".into()));
        }
        read_buf.extend_from_slice(&tmp[..n]);
    };

    // 3. Peek at first message in the record to determine version
    if server_hello_payload.len() < 4 {
        return Err(crate::Error::Tls("ServerHello record too short".into()));
    }
    let sh_msg_len = ((server_hello_payload[1] as usize) << 16)
        | ((server_hello_payload[2] as usize) << 8)
        | server_hello_payload[3] as usize;
    if server_hello_payload.len() < 4 + sh_msg_len {
        return Err(crate::Error::Tls("ServerHello message truncated".into()));
    }
    let server_hello_msg = &server_hello_payload[..4 + sh_msg_len];
    let version = codec::peek_server_hello_version(server_hello_msg)?;

    if version == 0x0303 {
        return Self::connect_tls12(tcp_stream, &client_random, server_hello_msg, remaining_after_sh);
    }

    // 4. TLS 1.3 path: feed ServerHello to handshake then continue
    let mut result = hs.handle_message(server_hello_msg, &mut init_record)?;
    let mut pos = 4 + sh_msg_len;
    while pos < server_hello_payload.len() {
        if pos + 4 > server_hello_payload.len() { break; }
        let msg_len = ((server_hello_payload[pos + 1] as usize) << 16)
            | ((server_hello_payload[pos + 2] as usize) << 8)
            | server_hello_payload[pos + 3] as usize;
        let msg_end = pos + 4 + msg_len;
        if msg_end > server_hello_payload.len() { break; }
        let msg_bytes = &server_hello_payload[pos..msg_end];
        result = hs.handle_message(msg_bytes, &mut init_record)?;
        pos = msg_end;
        if let tls13::handshake::HandshakeResult::Complete = result { break; }
    }

    read_buf = remaining_after_sh;
    // Continue existing TLS 1.3 loop (identical to original, but starting from read_buf)
    'outer: loop {
        loop {
            if read_buf.len() < 5 { break; }
            let rec_len = ((read_buf[3] as usize) << 8) | (read_buf[4] as usize);
            if read_buf.len() < 5 + rec_len { break; }
            let (ct, payload, consumed) = init_record.read_record(&read_buf)?;
            read_buf.drain(..consumed);
            match ct {
                CHANGE_CIPHER_SPEC => continue,
                ALERT => {
                    if payload.len() >= 2 {
                        return Err(crate::Error::Tls(format!(
                            "TLS alert: level={} desc={}", payload[0], payload[1]
                        )));
                    }
                    return Err(crate::Error::Tls("TLS alert received".into()));
                }
                HANDSHAKE => {
                    let mut pos = 0;
                    while pos < payload.len() {
                        if pos + 4 > payload.len() {
                            return Err(crate::Error::Tls("truncated handshake in record".into()));
                        }
                        let msg_len = ((payload[pos + 1] as usize) << 16)
                            | ((payload[pos + 2] as usize) << 8)
                            | (payload[pos + 3] as usize);
                        let msg_end = pos + 4 + msg_len;
                        if msg_end > payload.len() {
                            return Err(crate::Error::Tls("handshake message exceeds record".into()));
                        }
                        let msg_bytes = &payload[pos..msg_end];
                        let r = hs.handle_message(msg_bytes, &mut init_record)?;
                        pos = msg_end;
                        if let tls13::handshake::HandshakeResult::Complete = r { break 'outer; }
                    }
                }
                _ => {
                    return Err(crate::Error::Tls(format!(
                        "unexpected record type: 0x{:02x}", ct
                    )));
                }
            }
        }
        let n = tcp_stream.read(&mut tmp).map_err(crate::Error::Io)?;
        if n == 0 {
            return Err(crate::Error::Tls("connection closed during handshake".into()));
        }
        read_buf.extend_from_slice(&tmp[..n]);
    }

    // TLS 1.3: send CCS + Finished + install app keys
    let mut ccs = Vec::new();
    codec::encode_change_cipher_spec(&mut ccs);
    tcp_stream.write_all(&ccs).map_err(crate::Error::Io)?;
    let finished_msg = hs.encode_client_finished()?;
    let mut finished_record = Vec::new();
    init_record.write_encrypted(HANDSHAKE, &finished_msg, &mut finished_record)?;
    tcp_stream.write_all(&finished_record).map_err(crate::Error::Io)?;
    hs.install_app_keys(&mut init_record)?;

    Ok(TlsStream {
        tcp: tcp_stream,
        record: RecordLayerVariant::Tls13(init_record),
        read_buf,
        plaintext_buf: Vec::new(),
    })
}
```

- [ ] **Step 4: 实现 connect_tls12**

```rust
fn connect_tls12(
    mut tcp_stream: std::net::TcpStream,
    client_random: &[u8; 32],
    server_hello_msg: &[u8],
    mut read_buf: Vec<u8>,
) -> crate::Result<Self> {
    use tls12::handshake::{Tls12Handshake, Tls12HandshakeResult};

    // Parse server_random and cipher_suite from the already-decoded ServerHello
    let server_random;
    let cipher_suite;
    match codec::decode_handshake(server_hello_msg)? {
        codec::HandshakeMessage::ServerHello(sh) => {
            server_random = sh.random;
            cipher_suite = sh.cipher_suite;
        }
        _ => return Err(crate::Error::Tls("expected ServerHello".into())),
    }

    let mut hs = Tls12Handshake::new(client_random, &server_random, cipher_suite)?;
    let mut tls12_record = Tls12RecordLayer::new();
    let mut tmp = [0u8; 8192];

    loop {
        loop {
            if read_buf.len() < 5 { break; }
            let rec_len = ((read_buf[3] as usize) << 8) | (read_buf[4] as usize);
            if read_buf.len() < 5 + rec_len { break; }

            let (ct, payload, consumed) = tls12_record.read_record(&read_buf)?;
            read_buf.drain(..consumed);

            match ct {
                CHANGE_CIPHER_SPEC => {
                    hs.handle_server_ccs(&mut tls12_record)?;
                }
                ALERT => {
                    if payload.len() >= 2 {
                        return Err(crate::Error::Tls(format!(
                            "TLS alert: level={} desc={}", payload[0], payload[1]
                        )));
                    }
                    return Err(crate::Error::Tls("TLS alert received".into()));
                }
                HANDSHAKE => {
                    let mut pos = 0;
                    while pos < payload.len() {
                        if pos + 4 > payload.len() {
                            return Err(crate::Error::Tls("truncated TLS 1.2 handshake".into()));
                        }
                        let msg_len = ((payload[pos + 1] as usize) << 16)
                            | ((payload[pos + 2] as usize) << 8)
                            | payload[pos + 3] as usize;
                        let msg_end = pos + 4 + msg_len;
                        if msg_end > payload.len() {
                            return Err(crate::Error::Tls("TLS 1.2 HS msg exceeds record".into()));
                        }
                        let msg_bytes = &payload[pos..msg_end];
                        let result = hs.handle_message(msg_bytes, &mut tls12_record)?;
                        pos = msg_end;
                        match result {
                            Tls12HandshakeResult::Continue => {}
                            Tls12HandshakeResult::SendToServer(bytes) => {
                                tcp_stream.write_all(&bytes).map_err(crate::Error::Io)?;
                            }
                            Tls12HandshakeResult::Complete => {
                                return Ok(TlsStream {
                                    tcp: tcp_stream,
                                    record: RecordLayerVariant::Tls12(tls12_record),
                                    read_buf,
                                    plaintext_buf: Vec::new(),
                                });
                            }
                        }
                    }
                }
                _ => {
                    return Err(crate::Error::Tls(format!(
                        "unexpected TLS 1.2 record type: 0x{:02x}", ct
                    )));
                }
            }
        }
        let n = tcp_stream.read(&mut tmp).map_err(crate::Error::Io)?;
        if n == 0 {
            return Err(crate::Error::Tls("connection closed during TLS 1.2 handshake".into()));
        }
        read_buf.extend_from_slice(&tmp[..n]);
    }
}
```

- [ ] **Step 5: 更新 TlsStream Read/Write impl 使用 RecordLayerVariant**

将 `Read for TlsStream` 和 `Write for TlsStream` 中所有 `self.record.read_record`/`write_encrypted` 的调用改为通过 `RecordLayerVariant` 的 enum 分发（已在 Step 1 中定义了委托方法）。同时将 `Drop` 中的 close_notify 加一个 match。

- [ ] **Step 6: 编译并运行全量测试**

```bash
cargo build 2>&1 | grep "^error"
cargo test 2>&1 | tail -5
```

Expected: 零错误，所有测试通过。

- [ ] **Step 7: Commit**

```bash
git add src/core/net/tls/mod.rs
git commit -m "feat(tls): TlsStream::connect 版本协商 — TLS 1.3/1.2 自动分叉"
```

---

## Task 9: 集成测试 tls12_pure_rust_https_get

**Files:**
- Modify: `tests/core/net/tls_test.rs`

- [ ] **Step 1: 写集成测试（需要 full_test feature）**

在 `tests/core/net/tls_test.rs` 末尾追加：

```rust
/// Pure Rust TLS 1.2 handshake + HTTPS GET against baidu.com.
/// Verifies auto-negotiation: our ClientHello offers 1.3+1.2,
/// and baidu.com responds with TLS 1.2.
#[cfg(feature = "full_test")]
#[test]
fn tls12_pure_rust_https_get() {
    use std::io::{Read, Write};
    use viv::core::net::http::HttpRequest;
    use viv::core::net::tls::TlsStream;

    let host = "baidu.com";
    let mut tls = TlsStream::connect(host, 443).expect("TLS connect failed");

    let req = HttpRequest {
        method: "GET".into(),
        path: "/".into(),
        headers: vec![
            ("Host".into(), host.into()),
            ("User-Agent".into(), "viv/0.1".into()),
            ("Accept".into(), "*/*".into()),
            ("Connection".into(), "close".into()),
        ],
        body: None,
    };
    tls.write_all(&req.to_bytes()).expect("write failed");

    let mut response = Vec::new();
    let mut buf = [0u8; 4096];
    loop {
        let n = tls.read(&mut buf).unwrap_or(0);
        if n == 0 { break; }
        response.extend_from_slice(&buf[..n]);
    }

    assert!(!response.is_empty(), "Response was empty");
    let resp_str = String::from_utf8_lossy(&response);
    let first_line = resp_str.lines().next().unwrap_or("");
    assert!(
        first_line.starts_with("HTTP/"),
        "Expected HTTP response, got: {}", first_line,
    );
    println!("TLS 1.2 auto-negotiation: {} bytes, status: {}", response.len(), first_line);
}
```

- [ ] **Step 2: 运行集成测试**

```bash
cargo test --features full_test tls12_pure_rust_https_get -- --nocapture 2>&1 | tail -10
```

Expected: 测试通过，输出 `TLS 1.2 auto-negotiation: N bytes, status: HTTP/1.1 ...`

- [ ] **Step 3: 同时确认 TLS 1.3 连接未回归**

```bash
cargo test --features full_test tls13_pure_rust_https_get -- --nocapture 2>&1 | tail -5
```

Expected: 连接一个支持 TLS 1.3 的服务器（如 `api.anthropic.com`）成功，若仍报 protocol_version 则服务器本身不支持。

- [ ] **Step 4: 运行全量单元测试（无 full_test）**

```bash
cargo test 2>&1 | tail -5
```

Expected: `test result: ok. N passed; 0 failed`

- [ ] **Step 5: Commit**

```bash
git add tests/core/net/tls_test.rs
git commit -m "test(tls12): 集成测试 tls12_pure_rust_https_get"
```

---

## 实现注意事项

1. **禁止崩溃**：`src/` 中不得有 `unwrap`/`expect`/`panic`，错误一律用 `?` 传播或构造 `crate::Error::Tls(...)`。

2. **证书验证暂缺**：`ServerKeyExchange` 签名验证在 Task 7 中标注为"out of scope"。后续安全加固时需补充。

3. **TLS 1.3 原有 `connect` 逻辑**：Task 8 中的重构必须保证 `stream_test.rs` 中的现有测试全部通过。

4. **Finished transcript**：TLS 1.2 的 Finished 消息使用 `Sha256::hash(所有握手消息)` 的前12字节（经PRF），而非 TLS 1.3 的 HMAC；transcript 计算方式在 Task 7 中需仔细校对。
