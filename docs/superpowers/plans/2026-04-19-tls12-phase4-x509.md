# TLS 1.2 Phase 4: X.509 Certificate Parsing Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把现有的 TLS 1.3 `x509.rs` 空壳替换为 `X509Certificate<'a>` 零拷贝解析器，从 DER 编码证书中提取链验证所需字段（tbs_bytes / issuer_dn / subject_dn / validity / spki / SAN / basicConstraints / signature）。

**Architecture:** `X509Certificate<'a>` 借用原始 DER 缓冲，所有字段是 `&'a [u8]` / `&'a str`。`DateTime` 独立结构支持 UTCTime 与 GeneralizedTime 解析、按元组比较时间有效期。Wildcard 主机名匹配按 RFC 6125 §6.4.3 最左标签规则。复用 Phase 2 的 `Parser`，并给它加一个 `read_any_raw` 方法返回完整 TLV 字节（含 tag+length）。

**Tech Stack:** Rust 2024；复用 `core::asn1::Parser`、`core::net::tls::crypto::sha256`；零第三方 crate。

**Spec reference:** `docs/superpowers/specs/2026-04-19-tls12-phase4-x509-design.md`

---

## 全局原则

- **TDD**：每 Task 先写失败测试 → 跑测试验证失败 → 写最小实现 → 跑测试验证通过 → 提交
- **禁止崩溃**：`src/` 绝不 `.unwrap()`/`.expect(...)`/`panic!`/`unreachable!`/`todo!`/`unimplemented!`
- **测试命令**：`cargo test --test core_tests x509 2>&1 | tail -10`
- **提交节奏**：每 Task 独立 commit

---

## File Structure

| 文件 | 职责 | 新增/修改 |
|---|---|---|
| `src/core/net/tls/x509.rs` | 整体替换：`X509Certificate` + `DateTime` + parsers | 修改（从 38 LOC → ~500 LOC） |
| `src/core/asn1.rs` | 加 `Parser::read_any_raw` | 修改 (+~15 LOC) |
| `tests/core/net/tls/x509_test.rs` | 单元测试 + 真实自签证书向量 | 新增 (~300 LOC) |
| `tests/core/net/tls/mod.rs` | `pub mod x509_test;` | 修改 |

---

## 测试向量（已用本机 openssl 离线生成）

Key/cert 生成命令（参考）：

```bash
openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 3650 -nodes \
    -subj "/CN=test.example.com" \
    -addext "subjectAltName=DNS:test.example.com,DNS:*.example.com"
openssl x509 -in cert.pem -outform DER -out cert.der
```

Cert 属性：
- CN = `test.example.com`
- SAN = `DNS:test.example.com, DNS:*.example.com`
- notBefore = `260419012110Z` (UTCTime YY=26 → 2026)
- notAfter = `360416012110Z` (UTCTime YY=36 → 2036)
- Version = v3 (INTEGER 02 inside [0] EXPLICIT)
- Basic Constraints: `critical, CA:TRUE` (self-signed)
- 公钥 = 2048-bit RSA
- 签名算法 = sha256WithRSAEncryption

Task 1 会把 DER 作为 hex 常量写入测试文件。

---

### Task 1: 骨架 + 真实证书向量 + `DateTime` 类型

**Files:**
- Modify: `src/core/net/tls/x509.rs` (整体替换)
- Create: `tests/core/net/tls/x509_test.rs`
- Modify: `tests/core/net/tls/mod.rs`

- [ ] **Step 1: 整体替换 `src/core/net/tls/x509.rs` 为骨架**

```rust
//! X.509 certificate parsing.
//!
//! Zero-copy DER decoder for X.509 v3 certificates. Exposes the minimal
//! field set needed by Phase 6 chain validation. All values borrow from
//! the input DER buffer.

#![allow(dead_code)]

use crate::Error;

/// X.509 certificate — all fields borrow from the input DER buffer.
pub struct X509Certificate<'a> {
    pub raw: &'a [u8],
    pub tbs_bytes: &'a [u8],
    pub version: u32,
    pub serial: &'a [u8],
    pub signature_algorithm: &'a [u8],
    pub issuer_dn: &'a [u8],
    pub subject_dn: &'a [u8],
    pub not_before: DateTime,
    pub not_after: DateTime,
    pub spki: &'a [u8],
    pub san_dns_names: Vec<&'a str>,
    pub is_ca: Option<bool>,
    pub signature: &'a [u8],
}

/// UTC date and time, resolution to seconds.
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]
pub struct DateTime {
    pub year: u16,
    pub month: u8,
    pub day: u8,
    pub hour: u8,
    pub minute: u8,
    pub second: u8,
}
```

- [ ] **Step 2: 创建 `tests/core/net/tls/x509_test.rs` 并嵌入证书向量**

```rust
use viv::core::net::tls::x509::{DateTime, X509Certificate};

/// Self-signed RSA-2048 test certificate generated by openssl 3.0.15.
/// CN=test.example.com, SAN=DNS:test.example.com,*.example.com
/// Valid 2026-04-19 01:21:10 UTC to 2036-04-16 01:21:10 UTC.
const CERT_DER_HEX: &str = "\
308203433082022ba003020102021478ca833d92b4a4d8ccbf1fd88a168c31af555ab6300d06092a864886f70d01010b0500301b3119301706035504030c1074\
6573742e6578616d706c652e636f6d301e170d3236303431393031323131305a170d3336303431363031323131305a301b3119301706035504030c1074657374\
2e6578616d706c652e636f6d30820122300d06092a864886f70d01010105000382010f003082010a0282010100d574499a7b3114d73d1832c653c6d9c9cfddd2\
db93471e0bfb74b051b2c09777c9bbe7ee782b0ada6c4cc9eac1179c6e7e34a31fb04bff1d1f5254c90ca5926c84de12333e37dfc6330f481acb49734884ce29\
8bc51ae566cf6d1414922c92aa88fca6d686f70979747ee6cfb7ab1eb8a23f500e8d762d75d4ca895c5c520579ab67653c590e52f1d656571417f88821900e83\
d5f38ebe2d200965ff24176f1977677a1dcbf9cfe2ed1c4e0b3891f505fd8bdba7412386e523c537d188d70fe674e26cba0776788d8f5ed7e3a29d52484688df\
20e7b55ce5cee861af8296f08e966380653b612fbc919371f1a3f215a9f0d02ae5baf17aeb7133b0d2e93e7a350203010001a37f307d301d0603551d0e041604\
14736208bc0a9dae231eae60550a1b9db4fe76da73301f0603551d23041830168014736208bc0a9dae231eae60550a1b9db4fe76da73300f0603551d130101ff\
040530030101ff302a0603551d11042330218210746573742e6578616d706c652e636f6d820d2a2e6578616d706c652e636f6d300d06092a864886f70d01010b\
05000382010100c55a9206725b339e314528698c809de2fd28fd9647f024fafe59a1ab93ecd19c5826cda78586cb566e14c8ad297d5106e85bee2999808bd101\
543ea7ce2c24793ab62057900302fb784529996efc06ca7e4403163f3263fee1febb4550965200a7f4918ffb28b8f7dfafd22adae008f51485e9a1a27f070088\
1128e0f6e2615caf4964f1cd9a52121dc8183c3bbbe5b12a12cf67891c8d5c4d20c3337c4d3f8129fe118efaa9c79c712bd7e08f425db24747ac77a68e7ea77a\
affcd1d963eb9da2d01aa0b95a2225d1bfeca46f06fca89e3474611f97c49f949885eff4bcbcad9b40f54b10ec17ff10f00b59f86ad693bc8a7ce227e62e8978\
5fc70126270af6";

fn hex_decode(s: &str) -> Vec<u8> {
    let s: String = s.chars().filter(|c| !c.is_whitespace()).collect();
    (0..s.len())
        .step_by(2)
        .map(|i| u8::from_str_radix(&s[i..i + 2], 16).unwrap())
        .collect()
}

#[test]
fn cert_hex_decodes() {
    let der = hex_decode(CERT_DER_HEX);
    assert_eq!(der.len(), 839);
}

#[test]
fn datetime_ordering() {
    let earlier = DateTime {
        year: 2026,
        month: 4,
        day: 19,
        hour: 1,
        minute: 0,
        second: 0,
    };
    let later = DateTime {
        year: 2026,
        month: 4,
        day: 19,
        hour: 1,
        minute: 0,
        second: 1,
    };
    assert!(earlier < later);
}
```

- [ ] **Step 3: 加入 `tests/core/net/tls/mod.rs`**

在 `pub mod stream_test;` 前插入 `pub mod x509_test;`：

```rust
// TLS 1.3 test module root

pub mod codec_test;
pub mod crypto;
pub mod handshake_test;
pub mod key_schedule_test;
pub mod record_test;
pub mod rsa_test;
pub mod stream_test;
pub mod x509_test;
```

- [ ] **Step 4: 构建 + smoke**

Run: `cargo build 2>&1 | tail -3 && cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -5`
Expected: `Finished` 无 warning；`2 passed`。

- [ ] **Step 5: 提交**

```bash
git add src/core/net/tls/x509.rs tests/core/net/tls/x509_test.rs tests/core/net/tls/mod.rs
git commit -m "$(cat <<'EOF'
feat(x509): scaffold X.509 parser module

Replace the TLS 1.3 no-op stubs with X509Certificate and DateTime type
signatures. Embeds a real self-signed RSA-2048 test certificate (839
bytes DER, valid 2026-04-19 → 2036-04-16) as a hex constant. Parsing
logic lands in subsequent commits.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `DateTime` 解析（UTCTime / GeneralizedTime / now_utc）

**Files:**
- Modify: `src/core/net/tls/x509.rs`
- Modify: `tests/core/net/tls/x509_test.rs`

- [ ] **Step 1: 写失败测试**

追加到 `tests/core/net/tls/x509_test.rs`：

```rust
#[test]
fn parse_utc_time_2020s() {
    let dt = DateTime::from_utc_time("260419012110Z").unwrap();
    assert_eq!(dt, DateTime { year: 2026, month: 4, day: 19, hour: 1, minute: 21, second: 10 });
}

#[test]
fn parse_utc_time_year_boundary_2049() {
    // YY=49 → 2049
    let dt = DateTime::from_utc_time("490101000000Z").unwrap();
    assert_eq!(dt.year, 2049);
}

#[test]
fn parse_utc_time_year_boundary_1950() {
    // YY=50 → 1950
    let dt = DateTime::from_utc_time("500101000000Z").unwrap();
    assert_eq!(dt.year, 1950);
}

#[test]
fn parse_utc_time_rejects_wrong_length() {
    assert!(DateTime::from_utc_time("26041901211").is_err());
    assert!(DateTime::from_utc_time("260419012110").is_err()); // no Z
}

#[test]
fn parse_utc_time_rejects_non_z_suffix() {
    assert!(DateTime::from_utc_time("260419012110X").is_err());
}

#[test]
fn parse_utc_time_rejects_non_digit() {
    assert!(DateTime::from_utc_time("26041a012110Z").is_err());
}

#[test]
fn parse_utc_time_rejects_bad_month() {
    assert!(DateTime::from_utc_time("261319012110Z").is_err());
}

#[test]
fn parse_generalized_time_basic() {
    let dt = DateTime::from_generalized_time("20250102123456Z").unwrap();
    assert_eq!(dt, DateTime { year: 2025, month: 1, day: 2, hour: 12, minute: 34, second: 56 });
}

#[test]
fn parse_generalized_time_rejects_wrong_length() {
    assert!(DateTime::from_generalized_time("2025010212345Z").is_err());
}

#[test]
fn now_utc_has_plausible_year() {
    let dt = DateTime::now_utc();
    assert!(dt.year >= 2026 && dt.year < 2100);
    assert!(dt.month >= 1 && dt.month <= 12);
    assert!(dt.day >= 1 && dt.day <= 31);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -8`
Expected: 编译失败（`from_utc_time`、`from_generalized_time`、`now_utc` 不存在）。

- [ ] **Step 3: 实现 `DateTime` 方法**

追加到 `src/core/net/tls/x509.rs`：

```rust
impl DateTime {
    /// Parse 13-char UTCTime `YYMMDDHHMMSSZ`.
    /// Year mapping per RFC 5280 §4.1.2.5.1: YY < 50 → 20YY, YY ≥ 50 → 19YY.
    pub fn from_utc_time(s: &str) -> crate::Result<Self> {
        if s.len() != 13 {
            return Err(Error::Tls(format!(
                "UTCTime: expected 13 chars, got {}",
                s.len()
            )));
        }
        let b = s.as_bytes();
        if b[12] != b'Z' {
            return Err(Error::Tls("UTCTime: expected trailing 'Z'".to_string()));
        }
        let yy = two_digit(b, 0)?;
        let year: u16 = if yy < 50 { 2000 + yy as u16 } else { 1900 + yy as u16 };
        let month = two_digit(b, 2)?;
        let day = two_digit(b, 4)?;
        let hour = two_digit(b, 6)?;
        let minute = two_digit(b, 8)?;
        let second = two_digit(b, 10)?;
        validate_date_fields(month, day, hour, minute, second)?;
        Ok(DateTime { year, month, day, hour, minute, second })
    }

    /// Parse 15-char GeneralizedTime `YYYYMMDDHHMMSSZ`.
    pub fn from_generalized_time(s: &str) -> crate::Result<Self> {
        if s.len() != 15 {
            return Err(Error::Tls(format!(
                "GeneralizedTime: expected 15 chars, got {}",
                s.len()
            )));
        }
        let b = s.as_bytes();
        if b[14] != b'Z' {
            return Err(Error::Tls("GeneralizedTime: expected trailing 'Z'".to_string()));
        }
        let hi = two_digit(b, 0)? as u16;
        let lo = two_digit(b, 2)? as u16;
        let year = hi * 100 + lo;
        let month = two_digit(b, 4)?;
        let day = two_digit(b, 6)?;
        let hour = two_digit(b, 8)?;
        let minute = two_digit(b, 10)?;
        let second = two_digit(b, 12)?;
        validate_date_fields(month, day, hour, minute, second)?;
        Ok(DateTime { year, month, day, hour, minute, second })
    }

    /// Current UTC time from `SystemTime::now`. Returns 1970-01-01 00:00:00
    /// if the system clock is before the UNIX epoch (extremely unusual).
    pub fn now_utc() -> Self {
        use std::time::{SystemTime, UNIX_EPOCH};
        let dur = SystemTime::now()
            .duration_since(UNIX_EPOCH)
            .unwrap_or_default();
        let total_secs = dur.as_secs() as i64;
        let days = total_secs / 86400;
        let remainder = total_secs % 86400;
        let hour = (remainder / 3600) as u8;
        let minute = ((remainder % 3600) / 60) as u8;
        let second = (remainder % 60) as u8;
        let (year, month, day) = rata_die(days);
        DateTime {
            year: year as u16,
            month,
            day,
            hour,
            minute,
            second,
        }
    }
}

/// Read 2 ASCII digits at `b[offset..offset+2]` as a decimal u8.
fn two_digit(b: &[u8], offset: usize) -> crate::Result<u8> {
    let h = b.get(offset).copied().unwrap_or(0);
    let l = b.get(offset + 1).copied().unwrap_or(0);
    if !h.is_ascii_digit() || !l.is_ascii_digit() {
        return Err(Error::Tls(format!(
            "time: non-digit at offset {offset}"
        )));
    }
    Ok((h - b'0') * 10 + (l - b'0'))
}

fn validate_date_fields(
    month: u8,
    day: u8,
    hour: u8,
    minute: u8,
    second: u8,
) -> crate::Result<()> {
    if !(1..=12).contains(&month) {
        return Err(Error::Tls(format!("time: invalid month {month}")));
    }
    if !(1..=31).contains(&day) {
        return Err(Error::Tls(format!("time: invalid day {day}")));
    }
    if hour > 23 {
        return Err(Error::Tls(format!("time: invalid hour {hour}")));
    }
    if minute > 59 {
        return Err(Error::Tls(format!("time: invalid minute {minute}")));
    }
    if second > 60 {
        // 60 allowed for leap seconds
        return Err(Error::Tls(format!("time: invalid second {second}")));
    }
    Ok(())
}

/// Convert days-since-1970-01-01 to (year, month, day) in UTC.
/// Same Howard Hinnant date algorithm used by src/log/mod.rs::rata_die.
fn rata_die(days: i64) -> (i64, u8, u8) {
    let z = days + 719468;
    let era = (if z >= 0 { z } else { z - 146096 }) / 146097;
    let doe = z - era * 146097;
    let yoe = (doe - doe / 1460 + doe / 36524 - doe / 146096) / 365;
    let y = yoe + era * 400;
    let doy = doe - (365 * yoe + yoe / 4 - yoe / 100);
    let mp = (5 * doy + 2) / 153;
    let d = doy - (153 * mp + 2) / 5 + 1;
    let m = if mp < 10 { mp + 3 } else { mp - 9 };
    let y = y + if m <= 2 { 1 } else { 0 };
    (y, m as u8, d as u8)
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -8`
Expected: `12 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/net/tls/x509.rs tests/core/net/tls/x509_test.rs
git commit -m "feat(x509): DateTime parsing (UTCTime/GeneralizedTime) + now_utc

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: `Parser::read_any_raw`

**Files:**
- Modify: `src/core/asn1.rs`
- Modify: `tests/core/asn1_test.rs`

- [ ] **Step 1: 写失败测试**

追加到 `tests/core/asn1_test.rs`：

```rust
#[test]
fn read_any_raw_returns_complete_tlv() {
    // 02 01 05 = INTEGER 5 — raw should be the whole TLV
    let mut p = Parser::new(&[0x02, 0x01, 0x05, 0xaa, 0xbb]);
    let raw = p.read_any_raw().unwrap();
    assert_eq!(raw, &[0x02, 0x01, 0x05]);
    // Remaining bytes stay in parser
    assert_eq!(p.remaining(), &[0xaa, 0xbb]);
}

#[test]
fn read_any_raw_with_long_length() {
    // 04 82 01 00 <256 bytes> = OCTET STRING 256 bytes
    let mut tlv = vec![0x04, 0x82, 0x01, 0x00];
    tlv.extend_from_slice(&[0xff; 256]);
    let mut p = Parser::new(&tlv);
    let raw = p.read_any_raw().unwrap();
    assert_eq!(raw.len(), 4 + 256);
    assert_eq!(&raw[..4], &[0x04, 0x82, 0x01, 0x00]);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests asn1::read_any_raw 2>&1 | tail -6`
Expected: 编译失败（`read_any_raw` 不存在）。

- [ ] **Step 3: 实现 `read_any_raw`**

追加到 `src/core/asn1.rs` 的 `impl<'a> Parser<'a>` 内（紧随 `read_any` 之后）：

```rust
    /// Read one TLV and return the complete raw bytes (tag + length + value).
    /// Unlike `read_any`, which returns only the value, this is useful for
    /// things like X.509 TBSCertificate that needs to be fed as-is to a
    /// signature verifier.
    pub fn read_any_raw(&mut self) -> crate::Result<&'a [u8]> {
        let start = self.pos;
        let _ = self.read_any()?;
        Ok(&self.data[start..self.pos])
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests asn1 2>&1 | tail -6`
Expected: `62 passed; 0 failed`（60 原有 + 2 新加）。

- [ ] **Step 5: 提交**

```bash
git add src/core/asn1.rs tests/core/asn1_test.rs
git commit -m "feat(asn1): Parser::read_any_raw for full TLV bytes

Returns tag+length+value as a single slice. Needed by Phase 4 X.509
parser for tbs_bytes (signature covers tag+length+value).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: `X509Certificate::from_der` 核心字段解析

**Files:**
- Modify: `src/core/net/tls/x509.rs`
- Modify: `tests/core/net/tls/x509_test.rs`

- [ ] **Step 1: 写失败测试**

追加到 `tests/core/net/tls/x509_test.rs`：

```rust
#[test]
fn from_der_parses_version_and_serial() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    assert_eq!(cert.version, 2); // v3
    // serial is 20 bytes in the test cert
    assert_eq!(cert.serial.len(), 20);
    assert_eq!(cert.serial[0], 0x78);
}

#[test]
fn from_der_parses_validity() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // notBefore: 2026-04-19 01:21:10 UTC
    assert_eq!(cert.not_before, DateTime {
        year: 2026, month: 4, day: 19,
        hour: 1, minute: 21, second: 10
    });
    // notAfter: 2036-04-16 01:21:10 UTC
    assert_eq!(cert.not_after, DateTime {
        year: 2036, month: 4, day: 16,
        hour: 1, minute: 21, second: 10
    });
}

#[test]
fn from_der_exposes_tbs_bytes() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // tbs_bytes must start with SEQUENCE tag 0x30 and appear inside the cert
    assert_eq!(cert.tbs_bytes[0], 0x30);
    assert!(cert.tbs_bytes.len() > 0);
    assert!(cert.tbs_bytes.len() < cert.raw.len());
}

#[test]
fn from_der_exposes_spki() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // SPKI starts with SEQUENCE
    assert_eq!(cert.spki[0], 0x30);
    // Can feed to Phase 3 RsaPublicKey::from_spki
    use viv::core::net::tls::rsa::RsaPublicKey;
    let pk = RsaPublicKey::from_spki(cert.spki).unwrap();
    assert_eq!(pk.n_byte_len(), 256);
}

#[test]
fn from_der_exposes_issuer_and_subject_equal_for_self_signed() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    assert_eq!(cert.issuer_dn, cert.subject_dn);
    // Name SEQUENCE tag 0x30
    assert_eq!(cert.subject_dn[0], 0x30);
}

#[test]
fn from_der_exposes_signature() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // RSA-2048 signature = 256 bytes
    assert_eq!(cert.signature.len(), 256);
}

#[test]
fn from_der_rejects_truncated() {
    assert!(X509Certificate::from_der(&[0x30]).is_err());
    assert!(X509Certificate::from_der(&[]).is_err());
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -10`
Expected: 编译失败（`from_der` 不存在）。

- [ ] **Step 3: 实现 `from_der`**

追加到 `src/core/net/tls/x509.rs`：

```rust
impl<'a> X509Certificate<'a> {
    pub fn from_der(der: &'a [u8]) -> crate::Result<Self> {
        use crate::core::asn1::{Parser, Tag};

        let mut top = Parser::new(der);

        // Peek the certificate SEQUENCE to capture raw bytes
        let cert_bytes = {
            let start = 0usize;
            let _ = top.read_any_raw().map_err(|e| Error::Tls(format!("X509: {e}")))?;
            // But we consumed top; better: reset and just use `der` as raw.
            // Since Certificate SEQUENCE is the whole input, raw == der.
            &der[start..]
        };

        // Re-parse from scratch into the SEQUENCE body.
        let mut top = Parser::new(der);
        let cert_inner_raw = top
            .read_any_raw()
            .map_err(|e| Error::Tls(format!("X509: top: {e}")))?;
        // cert_inner_raw is the full Certificate SEQUENCE (including tag+length)
        // — equivalent to the whole input for a well-formed cert.
        if cert_inner_raw.len() != der.len() {
            return Err(Error::Tls(
                "X509: trailing bytes after Certificate".to_string(),
            ));
        }

        // Open the Certificate SEQUENCE
        let mut cert_seq = Parser::new(der);
        let mut certificate = cert_seq
            .read_sequence()
            .map_err(|e| Error::Tls(format!("X509: Certificate seq: {e}")))?;

        // TBSCertificate — capture raw for signature coverage, then parse it
        let tbs_bytes = certificate
            .read_any_raw()
            .map_err(|e| Error::Tls(format!("X509: TBS raw: {e}")))?;
        // Re-parse TBSCertificate internally.
        let mut tbs_outer = Parser::new(tbs_bytes);
        let mut tbs = tbs_outer
            .read_sequence()
            .map_err(|e| Error::Tls(format!("X509: TBS seq: {e}")))?;

        // version [0] EXPLICIT INTEGER DEFAULT v1
        let version: u32 = match tbs
            .read_optional_explicit(0)
            .map_err(|e| Error::Tls(format!("X509: version wrap: {e}")))?
        {
            Some(mut v) => {
                let bytes = v
                    .read_integer()
                    .map_err(|e| Error::Tls(format!("X509: version int: {e}")))?;
                if bytes.len() != 1 {
                    return Err(Error::Tls(format!(
                        "X509: version: unexpected length {}",
                        bytes.len()
                    )));
                }
                v.finish().map_err(|e| Error::Tls(format!("X509: version: {e}")))?;
                bytes[0] as u32
            }
            None => 0,
        };

        // serialNumber INTEGER
        let serial = tbs
            .read_integer()
            .map_err(|e| Error::Tls(format!("X509: serial: {e}")))?;

        // signature (tbs inner AlgorithmIdentifier) — skip for now
        let _ = tbs
            .read_any()
            .map_err(|e| Error::Tls(format!("X509: tbs sig algo: {e}")))?;

        // issuer Name (raw bytes)
        let issuer_dn = tbs
            .read_any_raw()
            .map_err(|e| Error::Tls(format!("X509: issuer: {e}")))?;

        // validity SEQUENCE { notBefore Time, notAfter Time }
        let mut validity = tbs
            .read_sequence()
            .map_err(|e| Error::Tls(format!("X509: validity: {e}")))?;
        let not_before = read_time(&mut validity)?;
        let not_after = read_time(&mut validity)?;
        validity.finish().map_err(|e| Error::Tls(format!("X509: validity end: {e}")))?;

        // subject Name (raw bytes)
        let subject_dn = tbs
            .read_any_raw()
            .map_err(|e| Error::Tls(format!("X509: subject: {e}")))?;

        // subjectPublicKeyInfo (raw bytes for Phase 3 consumption)
        let spki = tbs
            .read_any_raw()
            .map_err(|e| Error::Tls(format!("X509: SPKI: {e}")))?;

        // issuerUniqueID / subjectUniqueID / extensions — placeholder defaults
        let san_dns_names: Vec<&'a str> = Vec::new();
        let is_ca: Option<bool> = None;
        // (Task 5 will populate these from extensions.)

        // Consume any remaining tbs bytes (uniqueIDs + extensions will be added
        // in Task 5); for now just let tbs_outer ignore leftover.
        while !tbs.is_empty() {
            let _ = tbs
                .read_any()
                .map_err(|e| Error::Tls(format!("X509: tbs leftover: {e}")))?;
        }

        // Certificate outer signatureAlgorithm
        let mut sig_algo = certificate
            .read_sequence()
            .map_err(|e| Error::Tls(format!("X509: outer alg: {e}")))?;
        let signature_algorithm = sig_algo
            .read_oid()
            .map_err(|e| Error::Tls(format!("X509: outer alg OID: {e}")))?;
        while !sig_algo.is_empty() {
            let _ = sig_algo
                .read_any()
                .map_err(|e| Error::Tls(format!("X509: outer alg params: {e}")))?;
        }

        // Certificate signatureValue BIT STRING
        let sig_bits = certificate
            .read_bit_string()
            .map_err(|e| Error::Tls(format!("X509: signature: {e}")))?;
        if sig_bits.unused_bits != 0 {
            return Err(Error::Tls(format!(
                "X509: signature BIT STRING unused_bits = {}",
                sig_bits.unused_bits
            )));
        }

        certificate
            .finish()
            .map_err(|e| Error::Tls(format!("X509: trailing after signature: {e}")))?;

        let _ = cert_bytes; // suppress unused warning until needed

        Ok(X509Certificate {
            raw: der,
            tbs_bytes,
            version,
            serial,
            signature_algorithm,
            issuer_dn,
            subject_dn,
            not_before,
            not_after,
            spki,
            san_dns_names,
            is_ca,
            signature: sig_bits.bytes,
        })
    }
}

/// Read a Time (either UTCTime or GeneralizedTime) from `p`.
fn read_time(p: &mut crate::core::asn1::Parser<'_>) -> crate::Result<DateTime> {
    use crate::core::asn1::Tag;
    let tag = p
        .peek_tag()
        .map_err(|e| Error::Tls(format!("time: peek: {e}")))?;
    if tag == Tag::UTC_TIME {
        let s = p
            .read_utc_time()
            .map_err(|e| Error::Tls(format!("time: {e}")))?;
        DateTime::from_utc_time(s)
    } else if tag == Tag::GENERALIZED_TIME {
        let s = p
            .read_generalized_time()
            .map_err(|e| Error::Tls(format!("time: {e}")))?;
        DateTime::from_generalized_time(s)
    } else {
        Err(Error::Tls(format!(
            "time: unexpected tag {:?}",
            tag
        )))
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -10`
Expected: `19 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/net/tls/x509.rs tests/core/net/tls/x509_test.rs
git commit -m "$(cat <<'EOF'
feat(x509): core Certificate fields (version/serial/validity/spki/DN/signature)

Parses TBSCertificate outer structure excluding extensions (those land
in the next commit). tbs_bytes captures the full SEQUENCE for signature
coverage. SPKI bytes are ready to feed RsaPublicKey::from_spki.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Extensions 解析（SAN + BasicConstraints）

**Files:**
- Modify: `src/core/net/tls/x509.rs`
- Modify: `tests/core/net/tls/x509_test.rs`

- [ ] **Step 1: 写失败测试**

追加到 `tests/core/net/tls/x509_test.rs`：

```rust
#[test]
fn from_der_parses_san_dns_names() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // Test cert has SAN = test.example.com, *.example.com
    assert_eq!(cert.san_dns_names.len(), 2);
    assert!(cert.san_dns_names.contains(&"test.example.com"));
    assert!(cert.san_dns_names.contains(&"*.example.com"));
}

#[test]
fn from_der_parses_basic_constraints_ca_true() {
    // Self-signed openssl req -x509 default sets basicConstraints CA:TRUE
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    assert_eq!(cert.is_ca, Some(true));
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -8`
Expected: 失败（当前 `is_ca` 总是 None，`san_dns_names` 总是空）。

- [ ] **Step 3: 替换 Task 4 中的 extensions 跳过逻辑为实际解析**

在 `src/core/net/tls/x509.rs` 的 `from_der` 内，把 Task 4 写的：

```rust
        let san_dns_names: Vec<&'a str> = Vec::new();
        let is_ca: Option<bool> = None;

        while !tbs.is_empty() {
            let _ = tbs
                .read_any()
                .map_err(|e| Error::Tls(format!("X509: tbs leftover: {e}")))?;
        }
```

替换为：

```rust
        let mut san_dns_names: Vec<&'a str> = Vec::new();
        let mut is_ca: Option<bool> = None;

        // Skip optional issuerUniqueID [1] IMPLICIT BIT STRING
        let _ = tbs
            .read_optional(crate::core::asn1::Tag::context(1, false))
            .map_err(|e| Error::Tls(format!("X509: issuerUniqueID: {e}")))?;
        // Skip optional subjectUniqueID [2] IMPLICIT BIT STRING
        let _ = tbs
            .read_optional(crate::core::asn1::Tag::context(2, false))
            .map_err(|e| Error::Tls(format!("X509: subjectUniqueID: {e}")))?;

        // extensions [3] EXPLICIT SEQUENCE OF Extension (OPTIONAL)
        if let Some(mut ext_wrapper) = tbs
            .read_optional_explicit(3)
            .map_err(|e| Error::Tls(format!("X509: extensions wrapper: {e}")))?
        {
            let mut ext_seq = ext_wrapper
                .read_sequence()
                .map_err(|e| Error::Tls(format!("X509: extensions seq: {e}")))?;
            while !ext_seq.is_empty() {
                parse_one_extension(&mut ext_seq, &mut san_dns_names, &mut is_ca)?;
            }
            ext_wrapper
                .finish()
                .map_err(|e| Error::Tls(format!("X509: extensions end: {e}")))?;
        }

        // No trailing data in TBS
        if !tbs.is_empty() {
            return Err(Error::Tls(format!(
                "X509: {} bytes remain in TBS",
                tbs.remaining().len()
            )));
        }
```

然后在文件底部（`impl X509Certificate` 外）添加辅助函数：

```rust
/// OID 2.5.29.17 subjectAltName.
const OID_SAN: &[u8] = &[0x55, 0x1d, 0x11];
/// OID 2.5.29.19 basicConstraints.
const OID_BASIC_CONSTRAINTS: &[u8] = &[0x55, 0x1d, 0x13];
/// Context-specific [2] IA5String — dNSName variant of GeneralName.
const GENERAL_NAME_DNS_TAG: u8 = 0x82;

fn parse_one_extension<'a>(
    ext_seq: &mut crate::core::asn1::Parser<'a>,
    san_dns_names: &mut Vec<&'a str>,
    is_ca: &mut Option<bool>,
) -> crate::Result<()> {
    let mut ext = ext_seq
        .read_sequence()
        .map_err(|e| Error::Tls(format!("X509: ext seq: {e}")))?;
    let oid = ext
        .read_oid()
        .map_err(|e| Error::Tls(format!("X509: ext OID: {e}")))?;
    // critical BOOLEAN DEFAULT FALSE (optional)
    let _critical = match ext
        .read_optional(crate::core::asn1::Tag::BOOLEAN)
        .map_err(|e| Error::Tls(format!("X509: ext critical: {e}")))?
    {
        Some(b) if b.len() == 1 => b[0] != 0,
        Some(_) => {
            return Err(Error::Tls(
                "X509: ext critical BOOLEAN wrong length".to_string(),
            ));
        }
        None => false,
    };
    let ext_value_bytes = ext
        .read_octet_string()
        .map_err(|e| Error::Tls(format!("X509: ext value: {e}")))?;
    ext.finish()
        .map_err(|e| Error::Tls(format!("X509: ext trailing: {e}")))?;

    if oid == OID_SAN {
        parse_san(ext_value_bytes, san_dns_names)?;
    } else if oid == OID_BASIC_CONSTRAINTS {
        parse_basic_constraints(ext_value_bytes, is_ca)?;
    }
    // Unknown extensions (including critical) are ignored per spec.
    Ok(())
}

fn parse_san<'a>(
    ext_value: &'a [u8],
    san_dns_names: &mut Vec<&'a str>,
) -> crate::Result<()> {
    use crate::core::asn1::Parser;
    let mut p = Parser::new(ext_value);
    let mut names = p
        .read_sequence()
        .map_err(|e| Error::Tls(format!("SAN seq: {e}")))?;
    while !names.is_empty() {
        // Each entry is a GeneralName CHOICE — read raw (tag, value)
        let (tag, value) = names
            .read_any()
            .map_err(|e| Error::Tls(format!("SAN entry: {e}")))?;
        // dNSName is context-specific [2] IA5String (primitive); tag byte = 0x82
        if let Some(tb) = tag.to_short_byte() {
            if tb == GENERAL_NAME_DNS_TAG {
                let s = std::str::from_utf8(value).map_err(|e| {
                    Error::Tls(format!("SAN dNSName: invalid UTF-8: {e}"))
                })?;
                san_dns_names.push(s);
            }
            // Other GeneralName variants (rfc822, URI, etc.) are ignored.
        }
    }
    p.finish().map_err(|e| Error::Tls(format!("SAN trailing: {e}")))?;
    Ok(())
}

fn parse_basic_constraints(
    ext_value: &[u8],
    is_ca: &mut Option<bool>,
) -> crate::Result<()> {
    use crate::core::asn1::Parser;
    let mut p = Parser::new(ext_value);
    let mut bc = p
        .read_sequence()
        .map_err(|e| Error::Tls(format!("BasicConstraints seq: {e}")))?;
    // cA BOOLEAN DEFAULT FALSE (optional)
    let ca = match bc
        .read_optional(crate::core::asn1::Tag::BOOLEAN)
        .map_err(|e| Error::Tls(format!("BasicConstraints cA: {e}")))?
    {
        Some(b) if b.len() == 1 => b[0] != 0,
        Some(_) => {
            return Err(Error::Tls(
                "BasicConstraints cA BOOLEAN wrong length".to_string(),
            ));
        }
        None => false,
    };
    *is_ca = Some(ca);
    // pathLenConstraint skipped (optional).
    Ok(())
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -10`
Expected: `21 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/net/tls/x509.rs tests/core/net/tls/x509_test.rs
git commit -m "$(cat <<'EOF'
feat(x509): parse SubjectAltName and BasicConstraints

SAN limited to dNSName (context-specific [2] IA5String); other
GeneralName variants ignored. BasicConstraints extracts cA flag only.
Unknown extensions (including critical=true) ignored per spec.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: `matches_hostname` + `is_valid_at`

**Files:**
- Modify: `src/core/net/tls/x509.rs`
- Modify: `tests/core/net/tls/x509_test.rs`

- [ ] **Step 1: 写失败测试**

追加到 `tests/core/net/tls/x509_test.rs`：

```rust
#[test]
fn matches_hostname_exact() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    assert!(cert.matches_hostname("test.example.com"));
}

#[test]
fn matches_hostname_exact_case_insensitive() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    assert!(cert.matches_hostname("Test.Example.COM"));
}

#[test]
fn matches_hostname_wildcard_single_label() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // *.example.com matches foo.example.com
    assert!(cert.matches_hostname("foo.example.com"));
}

#[test]
fn matches_hostname_wildcard_rejects_multi_label() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // *.example.com does NOT match foo.bar.example.com
    assert!(!cert.matches_hostname("foo.bar.example.com"));
}

#[test]
fn matches_hostname_wildcard_rejects_parent() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // *.example.com does NOT match example.com
    assert!(!cert.matches_hostname("example.com"));
}

#[test]
fn matches_hostname_unrelated_rejected() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    assert!(!cert.matches_hostname("other.org"));
}

#[test]
fn is_valid_at_within_window() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    // notBefore 2026-04-19, notAfter 2036-04-16
    let now = DateTime { year: 2030, month: 6, day: 15, hour: 12, minute: 0, second: 0 };
    assert!(cert.is_valid_at(&now));
}

#[test]
fn is_valid_at_before_not_before() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    let past = DateTime { year: 2020, month: 1, day: 1, hour: 0, minute: 0, second: 0 };
    assert!(!cert.is_valid_at(&past));
}

#[test]
fn is_valid_at_after_not_after() {
    let der = hex_decode(CERT_DER_HEX);
    let cert = X509Certificate::from_der(&der).unwrap();
    let future = DateTime { year: 2050, month: 1, day: 1, hour: 0, minute: 0, second: 0 };
    assert!(!cert.is_valid_at(&future));
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -10`
Expected: 编译失败（`matches_hostname`、`is_valid_at` 不存在）。

- [ ] **Step 3: 实现两个方法**

追加到 `src/core/net/tls/x509.rs` 的 `impl<'a> X509Certificate<'a>` 内：

```rust
    /// Wildcard-aware hostname match against SAN dNSName entries.
    /// Leftmost-label wildcard per RFC 6125 §6.4.3; case-insensitive.
    pub fn matches_hostname(&self, hostname: &str) -> bool {
        let host_lc = hostname.to_ascii_lowercase();
        for san in &self.san_dns_names {
            if san_matches(san, &host_lc) {
                return true;
            }
        }
        false
    }

    /// Inclusive validity check: `not_before <= now <= not_after`.
    pub fn is_valid_at(&self, now: &DateTime) -> bool {
        &self.not_before <= now && now <= &self.not_after
    }
```

在文件底部加辅助函数：

```rust
/// SAN entry matcher per RFC 6125 §6.4.3. Leftmost-label wildcard only.
fn san_matches(san: &str, host_lc: &str) -> bool {
    let san_lc = san.to_ascii_lowercase();
    // Non-wildcard: exact match
    if !san_lc.starts_with("*.") {
        return san_lc == host_lc;
    }
    // Wildcard: "*.rest" matches "<one-label>.rest"
    let suffix = &san_lc[1..]; // includes the leading '.'
    if !host_lc.ends_with(suffix) {
        return false;
    }
    let prefix_len = host_lc.len() - suffix.len();
    if prefix_len == 0 {
        return false; // cannot match the bare domain
    }
    // Prefix must be a single label (no dots)
    !host_lc[..prefix_len].contains('.')
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests 'core::net::tls::x509_test' 2>&1 | tail -10`
Expected: `30 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/net/tls/x509.rs tests/core/net/tls/x509_test.rs
git commit -m "$(cat <<'EOF'
feat(x509): matches_hostname (RFC 6125) and is_valid_at

Leftmost-label wildcard matching; case-insensitive. Validity check is
inclusive on both endpoints.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 验收 + 推送

**Files:** (no source changes)

- [ ] **Step 1: 无崩溃扫描**

Run:
```bash
echo '=== src/core/net/tls/x509.rs ===' && \
grep -nE '\.unwrap\(\)|\.expect\(|panic!|unreachable!|todo!|unimplemented!' src/core/net/tls/x509.rs || echo 'clean'

echo '=== full src/ ===' && \
grep -rnE '\.unwrap\(\)|\.expect\("|panic!\(|unreachable!\(\)|todo!\(\)|unimplemented!\(\)' --include='*.rs' src/ || echo 'clean'
```
Expected: `x509.rs` clean；整树仅 `src/core/sync.rs` 文档注释命中。

- [ ] **Step 2: 完整构建 + 跑 X.509 测试**

Run: `cargo build --tests 2>&1 | tail -3 && cargo test --test core_tests x509 2>&1 | tail -5`
Expected: `Finished` 无新 warning；`30 passed; 0 failed`.

- [ ] **Step 3: 跑全量套件确保无回归**

Run: `cargo test 2>&1 | grep -E 'test result|FAIL' | head -25`
Expected: 所有 `test result: ok.`，无 `FAIL`。

- [ ] **Step 4: 推送**

```bash
git log --oneline origin/main..HEAD
git push origin main
```
Expected: Task 1-6 的 6 个 commit（+ 文档 + asn1 的 read_any_raw）推送上去。

---

## Self-Review

**Spec coverage**:

| Spec 项 | 覆盖 |
|---|---|
| `X509Certificate` 字段 (raw / tbs_bytes / version / serial / ... / signature) | Task 1 (声明) + Task 4 (填充) + Task 5 (SAN/is_ca) |
| `DateTime` 结构 + PartialOrd | Task 1 |
| `DateTime::from_utc_time` 含 YY 映射 | Task 2 |
| `DateTime::from_generalized_time` | Task 2 |
| `DateTime::now_utc` | Task 2 |
| `Parser::read_any_raw` | Task 3 |
| TBSCertificate 解析 | Task 4 |
| SAN 只提 dNSName | Task 5 |
| BasicConstraints 只提 cA | Task 5 |
| 未知扩展（含 critical）忽略 | Task 5 |
| 主机名 wildcard 匹配 | Task 6 |
| 有效期检查 | Task 6 |
| 替换旧 `verify_chain`/`verify_signature` stubs | Task 1（整体 replace 文件） |
| 无崩溃代码 + 无 warning + 全量测试 + 推送 | Task 7 |

**Placeholder scan**: 所有代码步骤均为完整可执行 Rust。测试向量已硬编码（839 字节 cert DER）。无 "TBD"、"implement later"、"similar to"、"handle edge cases"。

**Type consistency**:
- `X509Certificate<'a>` 生命周期全程一致
- `DateTime` 字段顺序（year / month / day / hour / minute / second）与 `PartialOrd`/`Ord` derive 一致
- `parse_one_extension` / `parse_san` / `parse_basic_constraints` 签名与调用点匹配
- `OID_SAN` / `OID_BASIC_CONSTRAINTS` / `GENERAL_NAME_DNS_TAG` 常量在 Task 5 一次定义、同 Task 使用

**已修微瑕**：Task 4 的 `from_der` 里 `cert_bytes = &der[0..]` 本质上就是 `raw = der`；我保留了显式结构以便代码可读性，但运行时等价。
