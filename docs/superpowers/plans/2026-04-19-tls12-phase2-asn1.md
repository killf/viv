# TLS 1.2 Phase 2: ASN.1/DER Parser Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现零拷贝的 ASN.1/DER 解析器，为 Phase 4 (X.509)、Phase 3 (RSA INTEGER 读取)、Phase 5 (ECDSA 签名) 提供解码层。

**Architecture:** Cursor 风格 `Parser<'a>` 遍历 `&'a [u8]`；所有 value 以借用切片返回；Tag 按语义拆解（class/constructed/number）；拒绝 BER indefinite length；失败返回 `Error::Asn1(String)`。

**Tech Stack:** Rust 2024 edition, zero dependencies; 新增 `src/core/asn1.rs`、`tests/core/asn1_test.rs`、`Error::Asn1` 变体。

**Spec reference:** `docs/superpowers/specs/2026-04-19-tls12-phase2-asn1-design.md`

---

## 全局原则

- **TDD**：每个 Task 先写失败测试 → 跑测试验证失败 → 写最小实现 → 跑测试验证通过 → 提交
- **禁止崩溃**：`src/` 绝不出现 `.unwrap()`/`.expect(...)`/`panic!`/`unreachable!`/`todo!`/`unimplemented!`
- **运行测试命令**：`cargo test --test core_tests asn1 2>&1 | tail -10`
- **提交节奏**：每个 Task 结束必须有独立 git commit

---

## File Structure

| 文件 | 职责 | 新增/修改 |
|---|---|---|
| `src/core/asn1.rs` | `Tag` + `BitString` + `Parser` | 新增 (~400 LOC) |
| `src/core/mod.rs` | 导出 `pub mod asn1;` | 修改 |
| `src/error.rs` | 新增 `Asn1(String)` 变体 + Display | 修改 |
| `tests/core/asn1_test.rs` | 单元测试 | 新增 (~250 LOC) |
| `tests/core/mod.rs` | 声明 `mod asn1_test;` | 修改 |

---

### Task 1: 模块骨架 + Error::Asn1 变体

**Files:**
- Create: `src/core/asn1.rs`
- Modify: `src/core/mod.rs`
- Modify: `src/error.rs`
- Create: `tests/core/asn1_test.rs`
- Modify: `tests/core/mod.rs`

- [ ] **Step 1: 新增 `Error::Asn1` 变体**

Modify `src/error.rs` — 在 `LockPoisoned` 与 `Invariant` 之间加 `Asn1`：

```rust
    /// QR code generation errors
    Qr(String),
    /// ASN.1/DER parse errors
    Asn1(String),
    /// Mutex poisoned (another thread panicked while holding the lock)
    LockPoisoned(String),
    /// Invariant violation (unexpected state that was previously a panic)
    Invariant(String),
```

在 `Display` 实现里对应加：

```rust
            Error::Qr(msg) => write!(f, "QR code error: {}", msg),
            Error::Asn1(msg) => write!(f, "ASN.1 error: {}", msg),
            Error::LockPoisoned(msg) => write!(f, "lock poisoned: {}", msg),
            Error::Invariant(msg) => write!(f, "invariant violation: {}", msg),
```

- [ ] **Step 2: 创建 `src/core/asn1.rs` 的最小骨架**

```rust
//! ASN.1/DER parser for X.509, RSA, and ECDSA signature decoding.
//!
//! DER is a deterministic subset of BER. We do NOT support indefinite-length
//! encoding (0x80). The parser is zero-copy: all values are returned as
//! borrowed `&'a [u8]` slices into the input buffer.
//!
//! Not constant-time. Failure returns `Error::Asn1(String)`; no panic paths.

use crate::Error;

/// Tag class (upper 2 bits of the tag octet).
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TagClass {
    Universal,
    Application,
    ContextSpecific,
    Private,
}

/// Semantic decomposition of an ASN.1 tag.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Tag {
    pub class: TagClass,
    pub constructed: bool,
    pub number: u32,
}
```

- [ ] **Step 3: 在 `src/core/mod.rs` 加模块声明**

修改 `src/core/mod.rs`，在 `pub mod bigint;` 下面加一行：

```rust
pub mod asn1;
pub mod bigint;
pub mod json;
pub mod jsonrpc;
pub mod net;
pub mod platform;
pub mod runtime;
pub mod sync;
pub mod terminal;
```

- [ ] **Step 4: 创建 `tests/core/asn1_test.rs` 骨架**

```rust
use viv::core::asn1::{Tag, TagClass};

#[test]
fn tag_class_discriminated() {
    assert_ne!(TagClass::Universal, TagClass::ContextSpecific);
}
```

- [ ] **Step 5: 加 `tests/core/mod.rs` 模块声明**

修改 `tests/core/mod.rs`，在 `mod bigint_test;` 后加 `mod asn1_test;`：

```rust
mod asn1_test;
mod bigint_test;
mod jsonrpc_test;
mod net;
mod runtime;
mod sync_test;
mod terminal;
```

- [ ] **Step 6: 构建与运行**

Run: `cargo build 2>&1 | tail -3 && cargo test --test core_tests asn1 2>&1 | tail -5`
Expected: `Finished` 无 warning；`1 passed`。

- [ ] **Step 7: 提交**

```bash
git add src/core/asn1.rs src/core/mod.rs src/error.rs tests/core/asn1_test.rs tests/core/mod.rs
git commit -m "$(cat <<'EOF'
feat(asn1): scaffold ASN.1/DER parser module

First slice of TLS 1.2 Phase 2. Adds Tag/TagClass types, Error::Asn1
variant, module wiring, smoke test. Parser and primitive readers land
in subsequent commits.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Tag 常量 + from_bytes / to_short_byte

**Files:**
- Modify: `src/core/asn1.rs`
- Modify: `tests/core/asn1_test.rs`

- [ ] **Step 1: 写失败测试**

Replace `tests/core/asn1_test.rs` contents with:

```rust
use viv::core::asn1::{Tag, TagClass};

#[test]
fn tag_class_discriminated() {
    assert_ne!(TagClass::Universal, TagClass::ContextSpecific);
}

#[test]
fn tag_constants() {
    assert_eq!(Tag::INTEGER.number, 2);
    assert_eq!(Tag::INTEGER.class, TagClass::Universal);
    assert!(!Tag::INTEGER.constructed);

    assert_eq!(Tag::SEQUENCE.number, 16);
    assert!(Tag::SEQUENCE.constructed);
    assert_eq!(Tag::SET.number, 17);
    assert!(Tag::SET.constructed);

    assert_eq!(Tag::BOOLEAN.number, 1);
    assert_eq!(Tag::BIT_STRING.number, 3);
    assert_eq!(Tag::OCTET_STRING.number, 4);
    assert_eq!(Tag::NULL.number, 5);
    assert_eq!(Tag::OID.number, 6);
    assert_eq!(Tag::UTF8_STRING.number, 12);
    assert_eq!(Tag::PRINTABLE_STRING.number, 19);
    assert_eq!(Tag::IA5_STRING.number, 22);
    assert_eq!(Tag::UTC_TIME.number, 23);
    assert_eq!(Tag::GENERALIZED_TIME.number, 24);
    assert_eq!(Tag::BMP_STRING.number, 30);
}

#[test]
fn tag_from_bytes_short_form_primitive() {
    // 0x02 = INTEGER
    let (tag, consumed) = Tag::from_bytes(&[0x02]).unwrap();
    assert_eq!(tag, Tag::INTEGER);
    assert_eq!(consumed, 1);
}

#[test]
fn tag_from_bytes_short_form_constructed() {
    // 0x30 = SEQUENCE (constructed, number=16)
    let (tag, consumed) = Tag::from_bytes(&[0x30]).unwrap();
    assert_eq!(tag, Tag::SEQUENCE);
    assert_eq!(consumed, 1);
}

#[test]
fn tag_from_bytes_context_specific() {
    // 0xA0 = context-specific, constructed, number=0 (i.e. [0] EXPLICIT)
    let (tag, consumed) = Tag::from_bytes(&[0xA0]).unwrap();
    assert_eq!(tag.class, TagClass::ContextSpecific);
    assert!(tag.constructed);
    assert_eq!(tag.number, 0);
    assert_eq!(consumed, 1);
}

#[test]
fn tag_from_bytes_high_tag_number() {
    // 0x1f = primitive, universal, high-tag-number form
    // Follow-up bytes: 0x81, 0x00 = number 128 (0b1 0000000)
    let (tag, consumed) = Tag::from_bytes(&[0x1f, 0x81, 0x00]).unwrap();
    assert_eq!(tag.class, TagClass::Universal);
    assert!(!tag.constructed);
    assert_eq!(tag.number, 128);
    assert_eq!(consumed, 3);
}

#[test]
fn tag_from_bytes_empty_is_err() {
    assert!(Tag::from_bytes(&[]).is_err());
}

#[test]
fn tag_from_bytes_truncated_high_tag() {
    // 0x1f indicates high-tag-number, but no follow-up bytes
    assert!(Tag::from_bytes(&[0x1f]).is_err());
    // Continuation byte (high bit set) but truncated
    assert!(Tag::from_bytes(&[0x1f, 0x81]).is_err());
}

#[test]
fn tag_to_short_byte_roundtrip() {
    assert_eq!(Tag::INTEGER.to_short_byte(), Some(0x02));
    assert_eq!(Tag::SEQUENCE.to_short_byte(), Some(0x30));
    assert_eq!(Tag::SET.to_short_byte(), Some(0x31));
    // Context-specific [0] EXPLICIT
    let ctx = Tag::context(0, true);
    assert_eq!(ctx.to_short_byte(), Some(0xA0));
}

#[test]
fn tag_to_short_byte_none_for_high_tag_number() {
    let big = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 31,
    };
    assert_eq!(big.to_short_byte(), None);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests asn1 2>&1 | tail -15`
Expected: 编译失败（大量常量/方法未定义）。

- [ ] **Step 3: 实现 Tag 常量、from_bytes、to_short_byte**

Replace `src/core/asn1.rs` contents with:

```rust
//! ASN.1/DER parser for X.509, RSA, and ECDSA signature decoding.
//!
//! DER is a deterministic subset of BER. We do NOT support indefinite-length
//! encoding (0x80). The parser is zero-copy: all values are returned as
//! borrowed `&'a [u8]` slices into the input buffer.
//!
//! Not constant-time. Failure returns `Error::Asn1(String)`; no panic paths.

use crate::Error;

/// Tag class (upper 2 bits of the tag octet).
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TagClass {
    Universal,
    Application,
    ContextSpecific,
    Private,
}

impl TagClass {
    fn from_bits(bits: u8) -> Self {
        match bits & 0b11 {
            0b00 => TagClass::Universal,
            0b01 => TagClass::Application,
            0b10 => TagClass::ContextSpecific,
            _ => TagClass::Private,
        }
    }

    fn to_bits(self) -> u8 {
        match self {
            TagClass::Universal => 0b00,
            TagClass::Application => 0b01,
            TagClass::ContextSpecific => 0b10,
            TagClass::Private => 0b11,
        }
    }
}

/// Semantic decomposition of an ASN.1 tag.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Tag {
    pub class: TagClass,
    pub constructed: bool,
    pub number: u32,
}

impl Tag {
    // Universal primitive
    pub const BOOLEAN: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 1,
    };
    pub const INTEGER: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 2,
    };
    pub const BIT_STRING: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 3,
    };
    pub const OCTET_STRING: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 4,
    };
    pub const NULL: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 5,
    };
    pub const OID: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 6,
    };
    pub const UTF8_STRING: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 12,
    };
    pub const PRINTABLE_STRING: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 19,
    };
    pub const IA5_STRING: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 22,
    };
    pub const UTC_TIME: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 23,
    };
    pub const GENERALIZED_TIME: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 24,
    };
    pub const BMP_STRING: Tag = Tag {
        class: TagClass::Universal,
        constructed: false,
        number: 30,
    };

    // Universal constructed
    pub const SEQUENCE: Tag = Tag {
        class: TagClass::Universal,
        constructed: true,
        number: 16,
    };
    pub const SET: Tag = Tag {
        class: TagClass::Universal,
        constructed: true,
        number: 17,
    };

    /// Context-specific tag builder.
    pub const fn context(number: u32, constructed: bool) -> Tag {
        Tag {
            class: TagClass::ContextSpecific,
            constructed,
            number,
        }
    }

    /// Decode a tag from the start of `input`. Returns (tag, bytes consumed).
    pub fn from_bytes(input: &[u8]) -> crate::Result<(Tag, usize)> {
        let first = *input
            .first()
            .ok_or_else(|| Error::Asn1("tag: empty input".to_string()))?;
        let class = TagClass::from_bits(first >> 6);
        let constructed = (first & 0b0010_0000) != 0;
        let low5 = first & 0b0001_1111;

        if low5 < 31 {
            return Ok((
                Tag {
                    class,
                    constructed,
                    number: low5 as u32,
                },
                1,
            ));
        }

        // High-tag-number form: accumulate 7 bits at a time from follow-up bytes
        // until a byte with high bit clear.
        let mut number: u32 = 0;
        let mut i = 1;
        loop {
            let b = *input
                .get(i)
                .ok_or_else(|| Error::Asn1("tag: truncated high-tag-number".to_string()))?;
            // Overflow check: 7 more bits → need number << 7 to fit in u32
            if number > (u32::MAX >> 7) {
                return Err(Error::Asn1("tag: number overflow".to_string()));
            }
            number = (number << 7) | ((b & 0x7f) as u32);
            i += 1;
            if b & 0x80 == 0 {
                break;
            }
        }
        Ok((
            Tag {
                class,
                constructed,
                number,
            },
            i,
        ))
    }

    /// Encode this tag as a single short-form byte. Returns `None` if the
    /// tag number is ≥ 31 (high-tag-number form required).
    pub fn to_short_byte(&self) -> Option<u8> {
        if self.number >= 31 {
            return None;
        }
        let mut b = self.class.to_bits() << 6;
        if self.constructed {
            b |= 0b0010_0000;
        }
        b |= self.number as u8 & 0x1f;
        Some(b)
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests asn1 2>&1 | tail -15`
Expected: `10 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/asn1.rs tests/core/asn1_test.rs
git commit -m "feat(asn1): Tag decomposition with semantic constants

Universal primitives (BOOLEAN/INTEGER/BIT_STRING/OCTET_STRING/NULL/OID/
UTF8_STRING/PRINTABLE_STRING/IA5_STRING/UTC_TIME/GENERALIZED_TIME/
BMP_STRING), constructed (SEQUENCE/SET), context-specific builder, and
from_bytes supporting high-tag-number form.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Parser 骨架 + read_length

**Files:**
- Modify: `src/core/asn1.rs`
- Modify: `tests/core/asn1_test.rs`

- [ ] **Step 1: 写失败测试**

Append to `tests/core/asn1_test.rs`:

```rust
use viv::core::asn1::Parser;

#[test]
fn parser_new_and_empty() {
    let p = Parser::new(&[]);
    assert!(p.is_empty());
    assert_eq!(p.remaining(), &[] as &[u8]);
}

#[test]
fn parser_new_nonempty() {
    let p = Parser::new(&[0x30, 0x00]);
    assert!(!p.is_empty());
    assert_eq!(p.remaining(), &[0x30, 0x00]);
}

#[test]
fn read_length_short_form() {
    // 0x05 = length 5
    let mut p = Parser::new(&[0x05]);
    let len = p.read_length_for_test().unwrap();
    assert_eq!(len, 5);
    assert!(p.is_empty());
}

#[test]
fn read_length_long_form_1byte() {
    // 0x81 0xff = length 255
    let mut p = Parser::new(&[0x81, 0xff]);
    let len = p.read_length_for_test().unwrap();
    assert_eq!(len, 255);
    assert!(p.is_empty());
}

#[test]
fn read_length_long_form_2byte() {
    // 0x82 0x01 0x00 = length 256
    let mut p = Parser::new(&[0x82, 0x01, 0x00]);
    let len = p.read_length_for_test().unwrap();
    assert_eq!(len, 256);
    assert!(p.is_empty());
}

#[test]
fn read_length_long_form_4byte() {
    // 0x84 0x00 0x01 0x00 0x00 = length 65536
    let mut p = Parser::new(&[0x84, 0x00, 0x01, 0x00, 0x00]);
    let len = p.read_length_for_test().unwrap();
    assert_eq!(len, 65536);
}

#[test]
fn read_length_rejects_indefinite() {
    // 0x80 = indefinite length (BER only, forbidden in DER)
    let mut p = Parser::new(&[0x80]);
    assert!(p.read_length_for_test().is_err());
}

#[test]
fn read_length_rejects_empty() {
    let mut p = Parser::new(&[]);
    assert!(p.read_length_for_test().is_err());
}

#[test]
fn read_length_rejects_truncated_long_form() {
    // 0x82 declares 2 follow-up bytes, only 1 present
    let mut p = Parser::new(&[0x82, 0x01]);
    assert!(p.read_length_for_test().is_err());
}

#[test]
fn read_length_rejects_over_4_bytes() {
    // 0x85 would need 5 follow-up bytes; we limit to 4 for usize-safety
    let mut p = Parser::new(&[0x85, 0x00, 0x00, 0x00, 0x00, 0x01]);
    assert!(p.read_length_for_test().is_err());
}
```

注：`read_length_for_test` 将是一个测试可见的 helper（`pub` 但命名显式），因为 length 解码本身是 parser 的私有细节，我们通过它验证行为。生产 API（`read_any` 等）在后续 task 中暴露。

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests asn1 2>&1 | tail -10`
Expected: 编译失败（`Parser`、`read_length_for_test` 不存在）。

- [ ] **Step 3: 实现 Parser 骨架 + read_length**

Append to `src/core/asn1.rs`:

```rust
/// Cursor-style DER parser over a borrowed byte slice.
pub struct Parser<'a> {
    data: &'a [u8],
    pos: usize,
}

impl<'a> Parser<'a> {
    pub fn new(data: &'a [u8]) -> Self {
        Parser { data, pos: 0 }
    }

    pub fn is_empty(&self) -> bool {
        self.pos >= self.data.len()
    }

    pub fn remaining(&self) -> &'a [u8] {
        &self.data[self.pos..]
    }

    /// Read a DER length encoding from the current position. Advances `pos`.
    /// Rejects indefinite length (0x80) and lengths needing >4 follow-up bytes.
    fn read_length(&mut self) -> crate::Result<usize> {
        let first = *self
            .data
            .get(self.pos)
            .ok_or_else(|| Error::Asn1("length: unexpected end of input".to_string()))?;
        self.pos += 1;

        if first < 0x80 {
            // Short form
            return Ok(first as usize);
        }
        if first == 0x80 {
            return Err(Error::Asn1(
                "length: indefinite form (BER) not allowed in DER".to_string(),
            ));
        }
        let n = (first & 0x7f) as usize;
        if n > 4 {
            return Err(Error::Asn1(format!(
                "length: long form with {n} follow-up bytes exceeds 4"
            )));
        }
        // Accumulate big-endian
        let mut len: usize = 0;
        for _ in 0..n {
            let b = *self
                .data
                .get(self.pos)
                .ok_or_else(|| Error::Asn1("length: truncated long form".to_string()))?;
            self.pos += 1;
            len = (len << 8) | (b as usize);
        }
        Ok(len)
    }

    /// Test-only accessor for `read_length`. Do not use in production code.
    #[doc(hidden)]
    pub fn read_length_for_test(&mut self) -> crate::Result<usize> {
        self.read_length()
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests asn1 2>&1 | tail -15`
Expected: `20 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/asn1.rs tests/core/asn1_test.rs
git commit -m "feat(asn1): Parser cursor + DER length decoding

Short form, long form up to 4 follow-up bytes. Rejects indefinite
length (BER-only), truncated input, and oversized long form. Internal
read_length is exposed via read_length_for_test for unit tests.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: read_any / read_expect / peek_tag

**Files:**
- Modify: `src/core/asn1.rs`
- Modify: `tests/core/asn1_test.rs`

- [ ] **Step 1: 写失败测试**

Append to `tests/core/asn1_test.rs`:

```rust
#[test]
fn read_any_simple_integer() {
    // 02 01 05 = INTEGER 5
    let mut p = Parser::new(&[0x02, 0x01, 0x05]);
    let (tag, value) = p.read_any().unwrap();
    assert_eq!(tag, Tag::INTEGER);
    assert_eq!(value, &[0x05]);
    assert!(p.is_empty());
}

#[test]
fn read_any_sequence_header() {
    // 30 03 02 01 05 = SEQUENCE { INTEGER 5 }
    let mut p = Parser::new(&[0x30, 0x03, 0x02, 0x01, 0x05]);
    let (tag, value) = p.read_any().unwrap();
    assert_eq!(tag, Tag::SEQUENCE);
    assert_eq!(value, &[0x02, 0x01, 0x05]);
    assert!(p.is_empty());
}

#[test]
fn read_any_truncated_value() {
    // declared length 5 but only 2 bytes follow
    let mut p = Parser::new(&[0x04, 0x05, 0xaa, 0xbb]);
    assert!(p.read_any().is_err());
}

#[test]
fn read_expect_matches() {
    let mut p = Parser::new(&[0x02, 0x01, 0x42]);
    let value = p.read_expect(Tag::INTEGER).unwrap();
    assert_eq!(value, &[0x42]);
}

#[test]
fn read_expect_mismatches() {
    // Expect INTEGER but see OCTET_STRING (0x04)
    let mut p = Parser::new(&[0x04, 0x01, 0xaa]);
    assert!(p.read_expect(Tag::INTEGER).is_err());
}

#[test]
fn peek_tag_does_not_advance() {
    let mut p = Parser::new(&[0x02, 0x01, 0x05]);
    let t = p.peek_tag().unwrap();
    assert_eq!(t, Tag::INTEGER);
    // After peek, we can still read_any
    let (tag, _value) = p.read_any().unwrap();
    assert_eq!(tag, Tag::INTEGER);
}

#[test]
fn peek_tag_empty_is_err() {
    let p = Parser::new(&[]);
    assert!(p.peek_tag().is_err());
}

#[test]
fn finish_consumed_ok() {
    let mut p = Parser::new(&[0x02, 0x01, 0x05]);
    let _ = p.read_any().unwrap();
    assert!(p.finish().is_ok());
}

#[test]
fn finish_leftover_errors() {
    let mut p = Parser::new(&[0x02, 0x01, 0x05, 0xff]);
    let _ = p.read_any().unwrap();
    assert!(p.finish().is_err());
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests asn1 2>&1 | tail -10`
Expected: 编译失败（`read_any`、`read_expect`、`peek_tag`、`finish` 不存在）。

- [ ] **Step 3: 实现这些方法**

Append to the `impl<'a> Parser<'a>` block in `src/core/asn1.rs`:

```rust
    /// Read one TLV, returning (tag, value bytes). Advances past the value.
    pub fn read_any(&mut self) -> crate::Result<(Tag, &'a [u8])> {
        // Decode tag (possibly multi-byte for high-tag-number form)
        let (tag, tag_bytes) = Tag::from_bytes(&self.data[self.pos..])?;
        self.pos += tag_bytes;

        // Decode length
        let len = self.read_length()?;

        // Slice out the value
        let end = self
            .pos
            .checked_add(len)
            .ok_or_else(|| Error::Asn1("value: length overflow".to_string()))?;
        if end > self.data.len() {
            return Err(Error::Asn1(format!(
                "value: truncated, need {} bytes, have {}",
                len,
                self.data.len() - self.pos
            )));
        }
        let value = &self.data[self.pos..end];
        self.pos = end;
        Ok((tag, value))
    }

    /// Peek at the next tag without advancing.
    pub fn peek_tag(&self) -> crate::Result<Tag> {
        let (tag, _consumed) = Tag::from_bytes(&self.data[self.pos..])?;
        Ok(tag)
    }

    /// Read one TLV and assert its tag matches `expected`. Returns the value
    /// bytes. On mismatch, does not rewind (the parser is in an error state).
    pub fn read_expect(&mut self, expected: Tag) -> crate::Result<&'a [u8]> {
        let (tag, value) = self.read_any()?;
        if tag != expected {
            return Err(Error::Asn1(format!(
                "expected tag {:?}, got {:?}",
                expected, tag
            )));
        }
        Ok(value)
    }

    /// Assert the parser has consumed all input. Used at the end of a
    /// top-level decode or a nested sub-parser.
    pub fn finish(self) -> crate::Result<()> {
        if self.pos != self.data.len() {
            return Err(Error::Asn1(format!(
                "finish: {} unconsumed bytes remain",
                self.data.len() - self.pos
            )));
        }
        Ok(())
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests asn1 2>&1 | tail -15`
Expected: `29 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/asn1.rs tests/core/asn1_test.rs
git commit -m "feat(asn1): read_any, peek_tag, read_expect, finish

Generic TLV decoding with explicit length bounds checking. finish()
enforces full consumption for top-level/sub-parser decoding.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: SEQUENCE / SET / EXPLICIT 子 parser

**Files:**
- Modify: `src/core/asn1.rs`
- Modify: `tests/core/asn1_test.rs`

- [ ] **Step 1: 写失败测试**

Append to `tests/core/asn1_test.rs`:

```rust
#[test]
fn read_sequence_yields_sub_parser() {
    // 30 06 02 01 01 02 01 02 = SEQUENCE { INTEGER 1, INTEGER 2 }
    let mut p = Parser::new(&[0x30, 0x06, 0x02, 0x01, 0x01, 0x02, 0x01, 0x02]);
    let mut seq = p.read_sequence().unwrap();
    let first = seq.read_expect(Tag::INTEGER).unwrap();
    assert_eq!(first, &[0x01]);
    let second = seq.read_expect(Tag::INTEGER).unwrap();
    assert_eq!(second, &[0x02]);
    assert!(seq.finish().is_ok());
    assert!(p.is_empty());
}

#[test]
fn read_sequence_wrong_tag_errors() {
    // 04 00 = OCTET_STRING empty -- not a SEQUENCE
    let mut p = Parser::new(&[0x04, 0x00]);
    assert!(p.read_sequence().is_err());
}

#[test]
fn read_set_yields_sub_parser() {
    // 31 03 02 01 07 = SET { INTEGER 7 }
    let mut p = Parser::new(&[0x31, 0x03, 0x02, 0x01, 0x07]);
    let mut s = p.read_set().unwrap();
    let v = s.read_expect(Tag::INTEGER).unwrap();
    assert_eq!(v, &[0x07]);
    assert!(s.finish().is_ok());
}

#[test]
fn read_explicit_unwraps() {
    // A0 03 02 01 05 = [0] EXPLICIT INTEGER 5
    let mut p = Parser::new(&[0xA0, 0x03, 0x02, 0x01, 0x05]);
    let mut inner = p.read_explicit(0).unwrap();
    let v = inner.read_expect(Tag::INTEGER).unwrap();
    assert_eq!(v, &[0x05]);
    assert!(inner.finish().is_ok());
}

#[test]
fn read_explicit_wrong_number_errors() {
    // A1 03 02 01 05 = [1] EXPLICIT INTEGER 5; we ask for [0]
    let mut p = Parser::new(&[0xA1, 0x03, 0x02, 0x01, 0x05]);
    assert!(p.read_explicit(0).is_err());
}

#[test]
fn read_optional_present() {
    // INTEGER 5 then OCTET_STRING 0xaa
    let mut p = Parser::new(&[0x02, 0x01, 0x05, 0x04, 0x01, 0xaa]);
    let value = p.read_optional(Tag::INTEGER).unwrap();
    assert_eq!(value, Some(&[0x05][..]));
    // Next read picks up OCTET_STRING
    let v2 = p.read_expect(Tag::OCTET_STRING).unwrap();
    assert_eq!(v2, &[0xaa]);
}

#[test]
fn read_optional_absent_does_not_advance() {
    // Only OCTET_STRING 0xaa
    let mut p = Parser::new(&[0x04, 0x01, 0xaa]);
    let value = p.read_optional(Tag::INTEGER).unwrap();
    assert_eq!(value, None);
    // Position unchanged
    let (tag, _) = p.read_any().unwrap();
    assert_eq!(tag, Tag::OCTET_STRING);
}

#[test]
fn read_optional_explicit_present() {
    // A0 03 02 01 05 (= [0] EXPLICIT INTEGER 5), then INTEGER 7
    let mut p = Parser::new(&[0xA0, 0x03, 0x02, 0x01, 0x05, 0x02, 0x01, 0x07]);
    let inner = p.read_optional_explicit(0).unwrap();
    assert!(inner.is_some());
    let mut inner = inner.unwrap();
    let v = inner.read_expect(Tag::INTEGER).unwrap();
    assert_eq!(v, &[0x05]);
    inner.finish().unwrap();
    let v2 = p.read_expect(Tag::INTEGER).unwrap();
    assert_eq!(v2, &[0x07]);
}

#[test]
fn read_optional_explicit_absent() {
    // Only INTEGER 7
    let mut p = Parser::new(&[0x02, 0x01, 0x07]);
    let inner = p.read_optional_explicit(0).unwrap();
    assert!(inner.is_none());
    let v = p.read_expect(Tag::INTEGER).unwrap();
    assert_eq!(v, &[0x07]);
}

#[test]
fn read_optional_at_end_of_input() {
    // Empty — peek should not error, just return None.
    let mut p = Parser::new(&[]);
    let v = p.read_optional(Tag::INTEGER).unwrap();
    assert_eq!(v, None);
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests asn1 2>&1 | tail -10`
Expected: 编译失败（`read_sequence`、`read_set`、`read_explicit`、`read_optional`、`read_optional_explicit` 不存在）。

- [ ] **Step 3: 实现构造型读取**

Append to `impl<'a> Parser<'a>`:

```rust
    /// Read a SEQUENCE and return a sub-parser over its contents.
    pub fn read_sequence(&mut self) -> crate::Result<Parser<'a>> {
        let value = self.read_expect(Tag::SEQUENCE)?;
        Ok(Parser::new(value))
    }

    /// Read a SET and return a sub-parser over its contents.
    pub fn read_set(&mut self) -> crate::Result<Parser<'a>> {
        let value = self.read_expect(Tag::SET)?;
        Ok(Parser::new(value))
    }

    /// Read a context-specific constructed [N] EXPLICIT wrapper and return a
    /// sub-parser over the wrapped value.
    pub fn read_explicit(&mut self, context_number: u32) -> crate::Result<Parser<'a>> {
        let expected = Tag::context(context_number, true);
        let value = self.read_expect(expected)?;
        Ok(Parser::new(value))
    }

    /// If the next TLV's tag matches `expected`, read and return its value.
    /// Otherwise, leave the position unchanged and return `None`. Also returns
    /// `None` if the parser is already at the end of input.
    pub fn read_optional(&mut self, expected: Tag) -> crate::Result<Option<&'a [u8]>> {
        if self.is_empty() {
            return Ok(None);
        }
        let (tag, _consumed) = Tag::from_bytes(&self.data[self.pos..])?;
        if tag != expected {
            return Ok(None);
        }
        let value = self.read_expect(expected)?;
        Ok(Some(value))
    }

    /// Like `read_explicit` but returns `None` if the next tag is not
    /// `[context_number] constructed`. Position advances only when matched.
    pub fn read_optional_explicit(
        &mut self,
        context_number: u32,
    ) -> crate::Result<Option<Parser<'a>>> {
        if self.is_empty() {
            return Ok(None);
        }
        let (tag, _consumed) = Tag::from_bytes(&self.data[self.pos..])?;
        let expected = Tag::context(context_number, true);
        if tag != expected {
            return Ok(None);
        }
        let inner = self.read_explicit(context_number)?;
        Ok(Some(inner))
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests asn1 2>&1 | tail -15`
Expected: `39 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/asn1.rs tests/core/asn1_test.rs
git commit -m "feat(asn1): SEQUENCE, SET, EXPLICIT, and optional readers

read_sequence/read_set return sub-parsers; read_explicit unwraps
context-specific constructed tags; read_optional and
read_optional_explicit consume only when the next tag matches.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Primitive 读取器（BOOL / NULL / OID / OCTET_STRING / INTEGER / BIT_STRING）

**Files:**
- Modify: `src/core/asn1.rs`
- Modify: `tests/core/asn1_test.rs`

- [ ] **Step 1: 写失败测试**

Append to `tests/core/asn1_test.rs`:

```rust
use viv::core::asn1::BitString;

#[test]
fn read_bool_true() {
    // 01 01 ff
    let mut p = Parser::new(&[0x01, 0x01, 0xff]);
    assert_eq!(p.read_bool().unwrap(), true);
}

#[test]
fn read_bool_false() {
    // 01 01 00
    let mut p = Parser::new(&[0x01, 0x01, 0x00]);
    assert_eq!(p.read_bool().unwrap(), false);
}

#[test]
fn read_bool_rejects_wrong_length() {
    // 01 02 00 00 -- BOOLEAN with 2-byte value is invalid DER
    let mut p = Parser::new(&[0x01, 0x02, 0x00, 0x00]);
    assert!(p.read_bool().is_err());
}

#[test]
fn read_null() {
    // 05 00
    let mut p = Parser::new(&[0x05, 0x00]);
    assert!(p.read_null().is_ok());
    assert!(p.is_empty());
}

#[test]
fn read_null_rejects_nonzero_length() {
    let mut p = Parser::new(&[0x05, 0x01, 0x00]);
    assert!(p.read_null().is_err());
}

#[test]
fn read_integer_positive() {
    // 02 01 05
    let mut p = Parser::new(&[0x02, 0x01, 0x05]);
    assert_eq!(p.read_integer().unwrap(), &[0x05]);
}

#[test]
fn read_integer_multi_byte() {
    // 02 02 01 00 = 256
    let mut p = Parser::new(&[0x02, 0x02, 0x01, 0x00]);
    assert_eq!(p.read_integer().unwrap(), &[0x01, 0x00]);
}

#[test]
fn read_oid_simple() {
    // 06 03 2a 86 48 = partial OID bytes
    let mut p = Parser::new(&[0x06, 0x03, 0x2a, 0x86, 0x48]);
    assert_eq!(p.read_oid().unwrap(), &[0x2a, 0x86, 0x48]);
}

#[test]
fn read_octet_string() {
    // 04 04 de ad be ef
    let mut p = Parser::new(&[0x04, 0x04, 0xde, 0xad, 0xbe, 0xef]);
    assert_eq!(p.read_octet_string().unwrap(), &[0xde, 0xad, 0xbe, 0xef]);
}

#[test]
fn read_bit_string_no_unused() {
    // 03 04 00 ab cd ef = BIT STRING, 0 unused bits, [ab,cd,ef]
    let mut p = Parser::new(&[0x03, 0x04, 0x00, 0xab, 0xcd, 0xef]);
    let bs = p.read_bit_string().unwrap();
    assert_eq!(bs.unused_bits, 0);
    assert_eq!(bs.bytes, &[0xab, 0xcd, 0xef]);
}

#[test]
fn read_bit_string_with_unused() {
    // 03 04 06 01 23 45 = unused=6, bytes=[0x01,0x23,0x45]
    let mut p = Parser::new(&[0x03, 0x04, 0x06, 0x01, 0x23, 0x45]);
    let bs = p.read_bit_string().unwrap();
    assert_eq!(bs.unused_bits, 6);
    assert_eq!(bs.bytes, &[0x01, 0x23, 0x45]);
}

#[test]
fn read_bit_string_rejects_empty_value() {
    // 03 00 -- value needs at least 1 byte for unused-bits prefix
    let mut p = Parser::new(&[0x03, 0x00]);
    assert!(p.read_bit_string().is_err());
}

#[test]
fn read_bit_string_rejects_unused_over_7() {
    // 03 02 08 ff = unused=8 is invalid per DER
    let mut p = Parser::new(&[0x03, 0x02, 0x08, 0xff]);
    assert!(p.read_bit_string().is_err());
}
```

Also add import path for `BitString` at the top of the test file near the other imports. Replace the existing `use viv::core::asn1::{Tag, TagClass};` line with:

```rust
use viv::core::asn1::{BitString, Parser, Tag, TagClass};
```

(merge the existing `use viv::core::asn1::Parser;` if it's separate)

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests asn1 2>&1 | tail -10`
Expected: 编译失败（`read_bool`、`read_null`、`read_integer`、`read_oid`、`read_octet_string`、`read_bit_string`、`BitString` 不存在）。

- [ ] **Step 3: 实现 BitString 和 primitive 读取器**

Add near the top of `src/core/asn1.rs` (after `Tag` impls, before `Parser`):

```rust
/// DER-encoded BIT STRING.
///
/// The first byte of the value in DER encoding is the number of unused bits
/// in the final byte (0-7). `bytes` is the payload after that prefix byte.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct BitString<'a> {
    pub unused_bits: u8,
    pub bytes: &'a [u8],
}
```

Append to `impl<'a> Parser<'a>`:

```rust
    pub fn read_bool(&mut self) -> crate::Result<bool> {
        let value = self.read_expect(Tag::BOOLEAN)?;
        if value.len() != 1 {
            return Err(Error::Asn1(format!(
                "BOOLEAN: expected 1-byte value, got {}",
                value.len()
            )));
        }
        Ok(value[0] != 0)
    }

    pub fn read_integer(&mut self) -> crate::Result<&'a [u8]> {
        self.read_expect(Tag::INTEGER)
    }

    pub fn read_null(&mut self) -> crate::Result<()> {
        let value = self.read_expect(Tag::NULL)?;
        if !value.is_empty() {
            return Err(Error::Asn1(format!(
                "NULL: expected empty value, got {} bytes",
                value.len()
            )));
        }
        Ok(())
    }

    pub fn read_oid(&mut self) -> crate::Result<&'a [u8]> {
        self.read_expect(Tag::OID)
    }

    pub fn read_octet_string(&mut self) -> crate::Result<&'a [u8]> {
        self.read_expect(Tag::OCTET_STRING)
    }

    pub fn read_bit_string(&mut self) -> crate::Result<BitString<'a>> {
        let value = self.read_expect(Tag::BIT_STRING)?;
        if value.is_empty() {
            return Err(Error::Asn1(
                "BIT STRING: missing unused-bits prefix byte".to_string(),
            ));
        }
        let unused_bits = value[0];
        if unused_bits > 7 {
            return Err(Error::Asn1(format!(
                "BIT STRING: invalid unused-bits value {unused_bits} (>7)"
            )));
        }
        Ok(BitString {
            unused_bits,
            bytes: &value[1..],
        })
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests asn1 2>&1 | tail -15`
Expected: `52 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/asn1.rs tests/core/asn1_test.rs
git commit -m "feat(asn1): primitive readers (BOOL/INTEGER/NULL/OID/OCTET/BIT_STRING)

BIT STRING carries the unused-bits prefix in a dedicated BitString<'a>
struct. BOOLEAN and NULL enforce strict DER length constraints.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: 字符串与时间读取器

**Files:**
- Modify: `src/core/asn1.rs`
- Modify: `tests/core/asn1_test.rs`

- [ ] **Step 1: 写失败测试**

Append to `tests/core/asn1_test.rs`:

```rust
#[test]
fn read_utf8_string_hello() {
    // 0c 05 68 65 6c 6c 6f
    let mut p = Parser::new(&[0x0c, 0x05, 0x68, 0x65, 0x6c, 0x6c, 0x6f]);
    assert_eq!(p.read_utf8_string().unwrap(), "hello");
}

#[test]
fn read_utf8_string_chinese() {
    // "你好" = e4 bd a0 e5 a5 bd (6 bytes)
    let mut p = Parser::new(&[0x0c, 0x06, 0xe4, 0xbd, 0xa0, 0xe5, 0xa5, 0xbd]);
    assert_eq!(p.read_utf8_string().unwrap(), "你好");
}

#[test]
fn read_utf8_string_rejects_invalid_utf8() {
    // 0c 02 ff fe -- invalid UTF-8 bytes
    let mut p = Parser::new(&[0x0c, 0x02, 0xff, 0xfe]);
    assert!(p.read_utf8_string().is_err());
}

#[test]
fn read_printable_string() {
    // 13 05 48 65 6c 6c 6f = "Hello"
    let mut p = Parser::new(&[0x13, 0x05, 0x48, 0x65, 0x6c, 0x6c, 0x6f]);
    assert_eq!(p.read_printable_string().unwrap(), "Hello");
}

#[test]
fn read_ia5_string() {
    // 16 03 61 62 63 = "abc"
    let mut p = Parser::new(&[0x16, 0x03, 0x61, 0x62, 0x63]);
    assert_eq!(p.read_ia5_string().unwrap(), "abc");
}

#[test]
fn read_utc_time() {
    // 17 0d 39 33 30 39 31 33 31 36 34 35 30 30 5a = "930913164500Z"
    let mut p = Parser::new(&[
        0x17, 0x0d, 0x39, 0x33, 0x30, 0x39, 0x31, 0x33, 0x31, 0x36, 0x34, 0x35, 0x30, 0x30, 0x5a,
    ]);
    assert_eq!(p.read_utc_time().unwrap(), "930913164500Z");
}

#[test]
fn read_generalized_time() {
    // 18 0f 32 30 32 35 30 31 30 32 31 32 33 34 35 36 5a = "20250102123456Z"
    let mut p = Parser::new(&[
        0x18, 0x0f, 0x32, 0x30, 0x32, 0x35, 0x30, 0x31, 0x30, 0x32, 0x31, 0x32, 0x33, 0x34, 0x35,
        0x36, 0x5a,
    ]);
    assert_eq!(p.read_generalized_time().unwrap(), "20250102123456Z");
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test --test core_tests asn1 2>&1 | tail -10`
Expected: 编译失败（字符串/时间方法未定义）。

- [ ] **Step 3: 实现字符串和时间读取器**

Append to `impl<'a> Parser<'a>`:

```rust
    pub fn read_utf8_string(&mut self) -> crate::Result<&'a str> {
        let bytes = self.read_expect(Tag::UTF8_STRING)?;
        std::str::from_utf8(bytes)
            .map_err(|e| Error::Asn1(format!("UTF8String: invalid UTF-8: {e}")))
    }

    pub fn read_printable_string(&mut self) -> crate::Result<&'a str> {
        let bytes = self.read_expect(Tag::PRINTABLE_STRING)?;
        // PrintableString is a subset of ASCII; UTF-8 validation covers it.
        std::str::from_utf8(bytes)
            .map_err(|e| Error::Asn1(format!("PrintableString: invalid ASCII: {e}")))
    }

    pub fn read_ia5_string(&mut self) -> crate::Result<&'a str> {
        let bytes = self.read_expect(Tag::IA5_STRING)?;
        std::str::from_utf8(bytes)
            .map_err(|e| Error::Asn1(format!("IA5String: invalid ASCII: {e}")))
    }

    pub fn read_utc_time(&mut self) -> crate::Result<&'a str> {
        let bytes = self.read_expect(Tag::UTC_TIME)?;
        std::str::from_utf8(bytes)
            .map_err(|e| Error::Asn1(format!("UTCTime: invalid ASCII: {e}")))
    }

    pub fn read_generalized_time(&mut self) -> crate::Result<&'a str> {
        let bytes = self.read_expect(Tag::GENERALIZED_TIME)?;
        std::str::from_utf8(bytes)
            .map_err(|e| Error::Asn1(format!("GeneralizedTime: invalid ASCII: {e}")))
    }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --test core_tests asn1 2>&1 | tail -15`
Expected: `59 passed; 0 failed`.

- [ ] **Step 5: 提交**

```bash
git add src/core/asn1.rs tests/core/asn1_test.rs
git commit -m "feat(asn1): string and time readers

UTF8String/PrintableString/IA5String/UTCTime/GeneralizedTime all return
borrowed &str after UTF-8 validation. Phase 4 parses the time format.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 真实证书片段 smoke + 最终验收

**Files:**
- Modify: `tests/core/asn1_test.rs`

- [ ] **Step 1: 写 smoke 测试**

Append to `tests/core/asn1_test.rs`:

```rust
/// Parse the outer structure of a real DER certificate head. This is the
/// first 34 bytes of ISRG Root X1 (Let's Encrypt's root CA), trimmed to
/// the TBSCertificate version field.
///
/// Structure: Certificate ::= SEQUENCE { tbsCertificate TBSCertificate, ... }
/// TBSCertificate ::= SEQUENCE { version [0] EXPLICIT Version, ... }
/// Version ::= INTEGER
///
/// We only verify the parser traverses the first three levels without error.
#[test]
fn parse_real_certificate_header() {
    // Hand-crafted DER equivalent of the prefix of a v3 certificate:
    //   30 82 05 6b          -- Certificate SEQUENCE, length 0x056b
    //     30 82 04 53        -- TBSCertificate SEQUENCE, length 0x0453
    //       a0 03            -- [0] EXPLICIT, length 3
    //         02 01 02       -- INTEGER 2 (version = v3)
    //       02 11 00 ff...   -- serialNumber INTEGER
    //
    // We construct just enough to exercise SEQUENCE -> SEQUENCE -> [0] EXPLICIT -> INTEGER.
    let der: &[u8] = &[
        0x30, 0x82, 0x00, 0x0c, // Certificate SEQUENCE, length 12
        0x30, 0x0a, // TBSCertificate SEQUENCE, length 10
        0xa0, 0x03, // [0] EXPLICIT, length 3
        0x02, 0x01, 0x02, // INTEGER 2
        0x02, 0x03, 0x01, 0x02, 0x03, // serialNumber INTEGER (dummy 3-byte)
    ];

    let mut top = Parser::new(der);
    let mut cert = top.read_sequence().unwrap();
    let mut tbs = cert.read_sequence().unwrap();
    let mut version_wrapper = tbs.read_explicit(0).unwrap();
    let version = version_wrapper.read_integer().unwrap();
    assert_eq!(version, &[0x02]);
    version_wrapper.finish().unwrap();
    let serial = tbs.read_integer().unwrap();
    assert_eq!(serial, &[0x01, 0x02, 0x03]);
    tbs.finish().unwrap();
    cert.finish().unwrap();
    top.finish().unwrap();
}
```

- [ ] **Step 2: 跑测试确认通过**

Run: `cargo test --test core_tests asn1 2>&1 | tail -10`
Expected: `60 passed; 0 failed`.

- [ ] **Step 3: 验收门禁**

Run the three verification commands in sequence:

```bash
echo '=== no-crash src/core/asn1.rs ===' && \
grep -nE '\.unwrap\(\)|\.expect\(|panic!|unreachable!|todo!|unimplemented!' src/core/asn1.rs || echo 'clean'

echo '=== full src/ no-crash scan ===' && \
grep -rnE '\.unwrap\(\)|\.expect\("|panic!\(|unreachable!\(\)|todo!\(\)|unimplemented!\(\)' --include='*.rs' src/ || echo 'clean'

echo '=== build ===' && cargo build --tests 2>&1 | tail -3
```
Expected:
- `asn1.rs` clean
- Only `src/core/sync.rs` doc-comment match
- Build succeeds, no new warnings

Run the asn1 test subset:

```bash
cargo test --test core_tests asn1 2>&1 | tail -5
```
Expected: `60 passed; 0 failed`.

Run the full test suite to ensure no regressions:

```bash
cargo test 2>&1 | grep -E 'test result|FAIL' | head -25
```
Expected: all `test result: ok.`, no `FAIL`.

- [ ] **Step 4: 推送**

```bash
git add tests/core/asn1_test.rs
git commit -m "test(asn1): real certificate header smoke test

Exercises SEQUENCE -> SEQUENCE -> [0] EXPLICIT -> INTEGER nesting,
which is the prefix pattern of every X.509 v3 certificate.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"

git log --oneline origin/main..HEAD
git push origin main
```

---

## Self-Review

**Spec coverage check**:

| Spec 项 | 覆盖 |
|---|---|
| `Error::Asn1(String)` 变体 | Task 1 |
| `TagClass` 四种类别 | Task 1 + Task 2 |
| `Tag` 结构 + 所有 universal 常量 | Task 2 |
| `Tag::context()` 构造器 | Task 2 |
| `Tag::from_bytes` (short + high-tag-number) | Task 2 |
| `Tag::to_short_byte` | Task 2 |
| `Parser::new/is_empty/remaining` | Task 3 |
| DER length decoding (short/long/reject indefinite) | Task 3 |
| `read_any` | Task 4 |
| `read_expect` | Task 4 |
| `peek_tag` | Task 4 |
| `finish` | Task 4 |
| `read_sequence` / `read_set` | Task 5 |
| `read_explicit` | Task 5 |
| `read_optional` / `read_optional_explicit` | Task 5 |
| `read_bool` / `read_integer` / `read_null` | Task 6 |
| `read_oid` / `read_octet_string` / `read_bit_string` | Task 6 |
| `BitString` 结构 | Task 6 |
| `read_utf8_string` / `read_printable_string` / `read_ia5_string` | Task 7 |
| `read_utc_time` / `read_generalized_time` | Task 7 |
| 真实证书片段 smoke | Task 8 |
| 无崩溃代码 + 全量测试 + 推送 | Task 8 |

**Placeholder scan**: 已检查。所有代码步骤都给出完整实现，无 "TBD"、"TODO"、"implement later"、"similar to"、"handle edge cases" 等占位表述。

**Type consistency check**:
- `Parser<'a>` 生命周期在所有 task 一致
- 所有 `read_*` 方法返回 `crate::Result<...>` 一致
- `BitString<'a>.unused_bits: u8` + `bytes: &'a [u8]` 与 Task 6 测试一致
- `Tag::context(number, constructed)` 参数顺序在 Task 2 定义后，Task 5 的 `read_explicit` 正确调用 `Tag::context(context_number, true)`
- `from_bytes -> Result<(Tag, usize)>` 在 Task 2 定义，Task 3 的 `read_any` 消费 `tag_bytes` 一致

No issues found.
